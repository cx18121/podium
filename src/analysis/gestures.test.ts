// src/analysis/gestures.test.ts
// VIS-04: Gesture detection stubs
// These are Wave 0 todo stubs — implementation comes in plan 02-02

import { describe, it } from 'vitest';
import type {} from './gestures';

describe('gestures (VIS-04)', () => {
  it.todo('detectFaceTouch returns face_touch event when fingertip is within threshold');
  it.todo('detectFaceTouch returns null when no hands present');
  it.todo('detectBodySway returns body_sway event when shoulder midpoint moves past threshold');
});
