// src/analysis/expressiveness.test.ts
// VIS-03: Expressiveness analysis stubs
// These are Wave 0 todo stubs — implementation comes in plan 02-02

import { describe, it } from 'vitest';
import type {} from './expressiveness';

describe('expressiveness (VIS-03)', () => {
  it.todo('returns higher score for animated blendshapes than flat blendshapes');
  it.todo('returns 0 for empty blendshapes array');
  it.todo('aggregateExpressiveness returns average of frame scores');
});
