---
phase: 07-visual-redesign
plan: 03
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - src/components/RecordingScreen/RecordingScreen.tsx
  - src/App.tsx
autonomous: true
requirements: [UI-REC-01, UI-APP-01]

must_haves:
  truths:
    - "RecordingScreen uses pure black background (#000) — intentionally darker than page bg"
    - "Timer is display-size (text-5xl/600/tabular-nums) in slate-100, not text-8xl mono light red"
    - "Stop button is red-500 (#ef4444) — red is valid here as a stop/danger signal"
    - "Processing spinner is indigo-colored, not gray-400"
    - "Naming screen wrapper uses #080c14 background"
  artifacts:
    - path: "src/components/RecordingScreen/RecordingScreen.tsx"
      provides: "Redesigned distraction-free recording view"
      contains: "text-5xl"
    - path: "src/App.tsx"
      provides: "Updated processing spinner + naming wrapper"
      contains: "text-[#6366f1]"
  key_links:
    - from: "src/components/RecordingScreen/RecordingScreen.tsx"
      to: "07-UI-SPEC.md RecordingScreen contract"
      via: "Black bg, display-size timer, red-500 stop btn"
      pattern: "bg-\\[#000\\]"
---

<objective>
Redesign RecordingScreen and the two App.tsx inline UI states (processing spinner + naming wrapper). RecordingScreen gets: pure black background, display-size tabular-nums timer, and a proper red-500 stop button. The processing spinner switches from gray-400 to indigo. The naming wrapper background matches the new page bg token.

Purpose: RecordingScreen is the most-used screen in the app — the timer and stop button must feel purposeful and high-contrast against pure black.
Output: `RecordingScreen.tsx` with Black/display-size/red-500 treatment; `App.tsx` processing state with indigo spinner and correct bg.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-UI-SPEC.md
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign RecordingScreen.tsx</name>
  <read_first>src/components/RecordingScreen/RecordingScreen.tsx</read_first>
  <files>src/components/RecordingScreen/RecordingScreen.tsx</files>
  <action>
Update `src/components/RecordingScreen/RecordingScreen.tsx`. Keep the component signature (`elapsedMs`, `onStop`, `formatElapsed`) identical. Change className strings only:

**Outer wrapper div:**
- Replace `bg-gray-950` with `bg-[#000]`
- Note: RecordingScreen spec says pure black (`#000`) — intentionally darker than the page bg (#080c14) because the camera-off distraction-free state should feel like a blackout

**Timer div (aria-live):**
Replace `text-8xl font-mono font-light tabular-nums text-red-400` with:
```
text-5xl font-semibold tabular-nums text-[#f1f5f9] tracking-tight
```
The timer text changes from oversized/mono/red to display-size/semibold/slate-100.

**Recording indicator row (the pulse dot + "RECORDING" text):** Keep as-is — the red pulse dot (`bg-red-500 animate-pulse`) and uppercase tracking label are correct per the minimal spec.

**Stop button:**
Replace `bg-gray-800 hover:bg-gray-700 text-white text-xl font-semibold rounded-2xl border border-gray-600 transition-colors` with:
```
px-10 h-[52px] bg-[#ef4444] hover:bg-[#f87171] text-white font-semibold rounded-xl
motion-safe:transition-colors motion-safe:duration-150
focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#ef4444] focus-visible:outline-offset-2
```
Also update the button label from `Stop` to `Stop Recording` (spec says "Stop Recording" for active state).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/RecordingScreen/RecordingScreen.tsx contains `bg-[#000]`
    - src/components/RecordingScreen/RecordingScreen.tsx contains `text-5xl font-semibold tabular-nums`
    - src/components/RecordingScreen/RecordingScreen.tsx contains `bg-[#ef4444]` (stop button)
    - src/components/RecordingScreen/RecordingScreen.tsx contains `Stop Recording` (button label)
    - src/components/RecordingScreen/RecordingScreen.tsx does NOT contain `text-8xl`
    - src/components/RecordingScreen/RecordingScreen.tsx does NOT contain `font-mono`
    - src/components/RecordingScreen/RecordingScreen.tsx does NOT contain `bg-gray-800` (old stop button)
    - All tests pass (npm test -- --run exits 0)
  </acceptance_criteria>
  <done>RecordingScreen: black bg, display-size semibold tabular-nums timer, red-500 stop button with "Stop Recording" label</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx processing spinner and naming wrapper</name>
  <read_first>src/App.tsx</read_first>
  <files>src/App.tsx</files>
  <action>
In `src/App.tsx`, update two inline UI states. Keep all logic, state, hooks, and handlers exactly as-is. Only change className strings in the `view === 'processing'` and `view === 'naming'` render blocks:

**Processing state div (view === 'processing'):**
The outer `<div>` currently has `bg-gray-950`. Change to `bg-[#080c14]`.
The spinner `<svg>` currently has `text-gray-400`. Change to `text-[#6366f1]`.
The `<p>` currently has `text-gray-400`. Change to `text-[#94a3b8]`.

Full updated processing block:
```tsx
if (view === 'processing') {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen bg-[#080c14] gap-4">
      <svg
        className="animate-spin w-8 h-8 text-[#6366f1]"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        aria-hidden="true"
      >
        <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
        <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z" />
      </svg>
      <p className="text-sm text-[#94a3b8]">Processing your recording...</p>
    </div>
  );
}
```

**Naming wrapper div (view === 'naming'):**
The outer `<div>` currently has `bg-gray-950`. Change to `bg-[#080c14]`.

Full updated naming block (wrapper only — NameSessionModal content unchanged):
```tsx
if (view === 'naming' && pendingRecording) {
  return (
    <div className="flex items-center justify-center min-h-screen bg-[#080c14]">
      <NameSessionModal
        autoTitle={pendingRecording.autoTitle}
        onConfirm={handleSaveName}
        onSkip={() => handleSaveName(pendingRecording.autoTitle)}
      />
    </div>
  );
}
```

Do not touch any other part of App.tsx.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `text-[#6366f1]` (indigo spinner color)
    - src/App.tsx processing div contains `bg-[#080c14]`
    - src/App.tsx naming wrapper div contains `bg-[#080c14]`
    - src/App.tsx does NOT contain `text-gray-400` in the processing block
    - All tests pass (npm test -- --run exits 0)
  </acceptance_criteria>
  <done>App.tsx processing spinner is indigo (#6366f1); both processing and naming wrapper divs use #080c14 background</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep "text-8xl" src/components/RecordingScreen/RecordingScreen.tsx` returns no match
- `grep "bg-\[#000\]" src/components/RecordingScreen/RecordingScreen.tsx` returns a match
- `grep "text-\[#6366f1\]" src/App.tsx` returns a match (spinner)
- `npm test -- --run` exits 0
</verification>

<success_criteria>
- RecordingScreen: pure black bg, text-5xl semibold tabular-nums timer, red-500 stop button
- App.tsx processing: indigo spinner, #080c14 background
- App.tsx naming: #080c14 wrapper background
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-03-SUMMARY.md`
</output>
