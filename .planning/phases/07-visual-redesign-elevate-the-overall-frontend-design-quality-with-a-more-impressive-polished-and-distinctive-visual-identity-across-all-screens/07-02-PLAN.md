---
phase: 07-visual-redesign
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - src/pages/Home.tsx
  - src/components/SetupScreen/SetupScreen.tsx
autonomous: true
requirements: [UI-HOME-01, UI-SETUP-01]

must_haves:
  truths:
    - "Home page shows 'Pitch Practice' wordmark with indigo underline accent"
    - "Each feature list item has a 6px indigo left-border accent bar instead of plain bullets"
    - "Start Recording CTA is indigo (#6366f1) with hover glow, not red"
    - "SetupScreen camera preview card uses dark surface (#111827) background"
    - "Privacy footnote uses muted text color (#475569)"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "Redesigned home/landing page"
      contains: "#6366f1"
    - path: "src/components/SetupScreen/SetupScreen.tsx"
      provides: "Redesigned setup screen with wordmark and indigo CTA"
      contains: "818cf8"
  key_links:
    - from: "src/pages/Home.tsx"
      to: "src/index.css"
      via: "bg-[#080c14] uses same value as --color-bg token"
      pattern: "080c14"
---

<objective>
Redesign the Home landing page and SetupScreen: replace gray-950 backgrounds, retire the red-600 CTA in favor of indigo, add the "Pitch Practice" wordmark with an indigo underline accent, add indigo left-border bars to feature list items, and update the camera preview card surface color.

Purpose: Home and Setup are the first screens a user sees — they establish the first impression of the redesigned identity.
Output: `src/pages/Home.tsx` and `src/components/SetupScreen/SetupScreen.tsx` with full Phase 7 visual treatment.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-UI-SPEC.md
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Home.tsx</name>
  <read_first>src/pages/Home.tsx</read_first>
  <files>src/pages/Home.tsx</files>
  <action>
Rewrite the JSX in `src/pages/Home.tsx`. Keep the component signature identical (`interface HomeProps`, `hasExistingSessions`, `onStart`). Change the following:

**Outer wrapper div:**
- Replace `bg-gray-950` with `bg-[#080c14]`

**Wordmark (h1):**
Replace the plain `<h1 className="text-xl font-semibold tracking-tight">Presentation Coach</h1>` with a wordmark block that has an indigo underline:
```tsx
<div className="flex flex-col items-center gap-1">
  <h1 className="text-xl font-semibold tracking-tight text-[#f1f5f9]">Pitch Practice</h1>
  <div className="h-[2px] w-6 bg-[#6366f1] rounded-full" aria-hidden="true" />
</div>
```

**Description paragraph:**
- Replace `text-gray-300` with `text-[#94a3b8]`
- Keep text content identical

**Feature list `<ul>`:**
- Replace `text-gray-400 text-sm space-y-2 text-left max-w-xs` with `space-y-2 text-left max-w-xs`
- Replace each `<li>` with a version that has an indigo left-border accent:
```tsx
<li className="flex items-center gap-3 text-[#94a3b8] text-sm">
  <span className="w-1.5 h-4 bg-[#6366f1] rounded-full flex-shrink-0" aria-hidden="true" />
  Eye contact tracking
</li>
```
Apply the same pattern to all 5 list items: "Eye contact tracking", "Filler word detection (um, uh, like, you know)", "Pacing and pause analysis", "Facial expressiveness score", "Nervous gesture detection".

**CTA button:**
Replace `bg-red-600 hover:bg-red-500 text-white text-lg font-semibold rounded-xl transition-colors` with:
```
px-8 h-[52px] bg-[#6366f1] hover:bg-[#818cf8] text-white font-semibold rounded-xl
motion-safe:transition-shadow motion-safe:duration-200
hover:shadow-[0_0_20px_rgba(99,102,241,0.3)]
active:scale-[0.98]
focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#6366f1] focus-visible:outline-offset-2
```

**Privacy footnote:**
- Replace `text-gray-500 text-xs` with `text-[#475569] text-[13px] tracking-wide`
- Keep text: "Runs entirely in your browser. Nothing is uploaded."
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Home.tsx contains `bg-[#080c14]`
    - src/pages/Home.tsx contains `Pitch Practice` (not "Presentation Coach")
    - src/pages/Home.tsx contains `bg-[#6366f1]` (indigo CTA)
    - src/pages/Home.tsx does NOT contain `bg-red-600`
    - src/pages/Home.tsx contains `w-1.5 h-4 bg-[#6366f1] rounded-full` (accent bars)
    - src/pages/Home.tsx contains `h-[2px] w-6 bg-[#6366f1]` (wordmark underline)
    - All tests pass (npm test -- --run exits 0)
  </acceptance_criteria>
  <done>Home.tsx fully redesigned — indigo wordmark with accent underline, indigo accent bars on feature list, indigo CTA with hover glow, muted footnote text</done>
</task>

<task type="auto">
  <name>Task 2: Redesign SetupScreen.tsx</name>
  <read_first>src/components/SetupScreen/SetupScreen.tsx</read_first>
  <files>src/components/SetupScreen/SetupScreen.tsx</files>
  <action>
Update `src/components/SetupScreen/SetupScreen.tsx`. Keep all logic (useEffect camera preview, error state, props) identical. Change only className strings and h1 content:

**Outer wrapper div:**
- Replace `bg-gray-950` with `bg-[#080c14]`

**Wordmark (h1):**
Replace `<h1 className="text-xl font-semibold tracking-tight">Presentation Coach</h1>` with:
```tsx
<div className="flex flex-col items-center gap-1">
  <h1 className="text-xl font-semibold tracking-tight text-[#f1f5f9]">Pitch Practice</h1>
  <div className="h-[2px] w-6 bg-[#6366f1] rounded-full" aria-hidden="true" />
</div>
```

**Camera preview card div:**
Replace `bg-gray-800 rounded-xl` with `bg-[#111827] border border-[rgba(255,255,255,0.07)] rounded-xl`

**Description paragraph:**
- Replace `text-gray-400 text-sm` with `text-[#94a3b8] text-sm`
- Keep text: "Check that you are in frame, then click Start Recording. The camera feed will be hidden during your session."

**CTA button (Start Recording):**
Replace `bg-red-600 hover:bg-red-500 text-white text-lg font-semibold rounded-xl transition-colors` with:
```
px-8 h-[52px] bg-[#6366f1] hover:bg-[#818cf8] text-white font-semibold rounded-xl
motion-safe:transition-shadow motion-safe:duration-200
hover:shadow-[0_0_20px_rgba(99,102,241,0.3)]
active:scale-[0.98]
focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#6366f1] focus-visible:outline-offset-2
```

**View History button:**
The existing `→ View History` button already has the Phase 05 treatment (no underline, arrow prefix, px-4 py-2). Update color classes:
- Replace `text-gray-400 hover:text-white` with `text-[#94a3b8] hover:text-[#f1f5f9]`
- Keep: `text-sm transition-colors px-4 py-2`
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SetupScreen/SetupScreen.tsx contains `bg-[#080c14]`
    - src/components/SetupScreen/SetupScreen.tsx contains `Pitch Practice`
    - src/components/SetupScreen/SetupScreen.tsx contains `bg-[#6366f1]` (indigo CTA)
    - src/components/SetupScreen/SetupScreen.tsx does NOT contain `bg-red-600`
    - src/components/SetupScreen/SetupScreen.tsx contains `bg-[#111827] border border-[rgba(255,255,255,0.07)]` (preview card)
    - src/components/SetupScreen/SetupScreen.tsx contains `h-[2px] w-6 bg-[#6366f1]` (wordmark underline)
    - All tests pass (npm test -- --run exits 0)
  </acceptance_criteria>
  <done>SetupScreen fully redesigned — Pitch Practice wordmark, indigo CTA, dark surface camera preview card</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep "bg-red-600" src/pages/Home.tsx` returns no match
- `grep "bg-red-600" src/components/SetupScreen/SetupScreen.tsx` returns no match
- `grep "6366f1" src/pages/Home.tsx` returns multiple matches (CTA + accent bars + underline)
- `npm test -- --run` exits 0
</verification>

<success_criteria>
- Both Home and SetupScreen: bg-[#080c14] page background
- Both: "Pitch Practice" wordmark with 2px indigo underline
- Both: Indigo CTA with hover glow — no red-600 CTA remaining
- Home: feature list items have indigo left-border accent bars
- SetupScreen: camera preview card uses #111827 surface with border
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-02-SUMMARY.md`
</output>
