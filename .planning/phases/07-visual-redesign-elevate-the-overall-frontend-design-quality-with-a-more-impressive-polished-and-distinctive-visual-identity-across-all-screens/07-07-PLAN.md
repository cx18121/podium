---
phase: 07-visual-redesign
plan: 07
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/components/NameSessionModal/NameSessionModal.tsx
  - src/components/DeleteConfirmModal/DeleteConfirmModal.tsx
  - src/components/common/SpeechSupportBanner.tsx
autonomous: true
requirements: [UI-MODAL-01, UI-BANNER-01]

must_haves:
  truths:
    - "NameSessionModal panel is #111827 with 20px radius and indigo save button"
    - "NameSessionModal input focus ring is indigo (not red)"
    - "NameSessionModal skip link reads 'Skip — use date/time name' with no underline"
    - "DeleteConfirmModal heading reads 'Delete this session?' and body copy matches spec"
    - "DeleteConfirmModal confirm button is red-500 (destructive), cancel reads 'Keep Session'"
    - "SpeechSupportBanner uses #451a03 bg with amber-400 3px left border"
    - "SpeechSupportBanner has a dismiss button (×) with 44px tap area"
  artifacts:
    - path: "src/components/NameSessionModal/NameSessionModal.tsx"
      provides: "Redesigned naming modal with indigo CTA and skip link"
      contains: "#6366f1"
    - path: "src/components/DeleteConfirmModal/DeleteConfirmModal.tsx"
      provides: "Updated delete confirmation with correct copy and red-500 destructive button"
      contains: "Delete this session?"
    - path: "src/components/common/SpeechSupportBanner.tsx"
      provides: "Redesigned amber warning banner with dismiss capability"
      contains: "#451a03"
  key_links:
    - from: "src/components/common/SpeechSupportBanner.tsx"
      to: "useState dismissed"
      via: "click dismiss button sets dismissed=true, returns null"
      pattern: "dismissed.*true"
---

<objective>
Redesign the three overlay/banner components: NameSessionModal, DeleteConfirmModal, and SpeechSupportBanner. These are the smallest surface area but require precise spec adherence. SpeechSupportBanner gets a new dismiss button (small logic addition — the only logic change in Phase 7).

Purpose: Modals are high-focus moments that must feel premium. The banner must follow the amber warning color system, not the old yellow-900 treatment.
Output: Three files redesigned with Phase 7 visual treatment.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-UI-SPEC.md
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign NameSessionModal.tsx and DeleteConfirmModal.tsx</name>
  <read_first>
    src/components/NameSessionModal/NameSessionModal.tsx
    src/components/DeleteConfirmModal/DeleteConfirmModal.tsx
  </read_first>
  <files>src/components/NameSessionModal/NameSessionModal.tsx, src/components/DeleteConfirmModal/DeleteConfirmModal.tsx</files>
  <action>
**NameSessionModal.tsx — changes (keep all logic: useState name, handleSave, handleSkip, props interface unchanged):**

1. Overlay div: keep `fixed inset-0 flex items-center justify-center z-50 p-4`. Replace `bg-black/70` with `bg-[rgba(0,0,0,0.7)]` (explicit alpha — same effect, matches depth contract).

2. Panel div: replace `bg-gray-900 rounded-2xl p-8 w-full max-w-md flex flex-col gap-6 shadow-2xl` with:
```
bg-[#111827] border border-[rgba(255,255,255,0.10)] rounded-[20px] p-8 w-full max-w-sm
flex flex-col gap-6 shadow-[0_25px_50px_rgba(0,0,0,0.6)]
```
Note: spec says 20px radius (`rounded-[20px]`), max-w-sm, 32px padding (p-8 = 32px).

3. Title h2: replace `text-xl font-bold text-white` with `text-xl font-semibold text-[#f1f5f9]` — change font-bold to font-semibold (spec caps at 600, font-bold=700).

4. Description paragraph: replace `text-gray-400 text-sm` with `text-[#94a3b8] text-sm`. Keep the existing text content (or remove the paragraph entirely — spec does not show a subtitle on the modal, only title + input + CTA + skip link). Remove the description paragraph that says "Give your session a name, or skip to use the auto-generated date and time." — spec is minimal.

5. Input field: replace `bg-gray-800 border border-gray-600 rounded-xl text-white placeholder-gray-500 focus:outline-none focus:border-red-600 transition-colors` with:
```
bg-[#1a2235] border border-[rgba(255,255,255,0.10)] rounded-lg text-[#f1f5f9]
placeholder-[#475569] focus:outline-none focus:border-[#6366f1]
focus:ring-2 focus:ring-[#6366f1] focus:ring-offset-0 motion-safe:transition-colors motion-safe:duration-150
```

6. Button row: replace `flex gap-3 justify-end` with `flex flex-col gap-3` (save CTA full-width, skip link below).

7. Save CTA button: replace `px-6 py-2.5 bg-red-600 hover:bg-red-500 disabled:opacity-40 disabled:cursor-not-allowed text-white font-semibold rounded-xl transition-colors text-sm` with:
```
w-full h-[48px] bg-[#6366f1] hover:bg-[#818cf8] disabled:opacity-40 disabled:cursor-not-allowed
text-white font-semibold rounded-xl motion-safe:transition-shadow motion-safe:duration-200
hover:shadow-[0_0_20px_rgba(99,102,241,0.3)]
focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#6366f1] focus-visible:outline-offset-2
```

8. Skip button: replace `px-5 py-2.5 text-gray-400 hover:text-white transition-colors text-sm font-medium` with `text-[13px] text-[#94a3b8] hover:text-[#f1f5f9] motion-safe:transition-colors motion-safe:duration-150 text-center no-underline`. Change skip button label from "Use auto name" to "Skip — use date/time name".

---

**DeleteConfirmModal.tsx — changes (keep all logic: onConfirm, onCancel props unchanged):**

1. Overlay div: replace `bg-black/60` with `bg-[rgba(0,0,0,0.7)]`

2. Panel div: replace `bg-gray-900 rounded-2xl p-6 max-w-sm w-full flex flex-col gap-4` with:
```
bg-[#111827] border border-[rgba(255,255,255,0.10)] rounded-[20px] p-6 max-w-sm w-full
flex flex-col gap-4 shadow-[0_25px_50px_rgba(0,0,0,0.6)]
```

3. Title h2: replace `text-white font-bold text-lg` with `text-[#f1f5f9] font-semibold text-xl` (font-bold→font-semibold per 2-weight rule)

4. Description p: replace current text `"This will permanently remove the recording and all coaching data. This cannot be undone."` with the spec copy: `"This permanently removes the recording and its scorecard. This cannot be undone."`. Update text class: replace `text-gray-400 text-sm` with `text-[#94a3b8] text-sm`.

5. Cancel button: replace `bg-gray-700 hover:bg-gray-600 text-white text-sm rounded-lg transition-colors` with:
```
px-4 py-2 bg-[#1a2235] hover:bg-[#111827] border border-[rgba(255,255,255,0.07)]
text-[#f1f5f9] text-sm rounded-lg motion-safe:transition-colors motion-safe:duration-150
focus-visible:outline focus-visible:outline-2 focus-visible:outline-[rgba(255,255,255,0.3)] focus-visible:outline-offset-1
```

6. Confirm (delete) button: replace `bg-red-600 hover:bg-red-500 text-white text-sm rounded-lg transition-colors` with `px-4 py-2 bg-[#ef4444] hover:bg-[#f87171] text-white text-sm font-semibold rounded-lg motion-safe:transition-colors motion-safe:duration-150 focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#ef4444] focus-visible:outline-offset-1`
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NameSessionModal/NameSessionModal.tsx contains `bg-[#111827]`
    - src/components/NameSessionModal/NameSessionModal.tsx contains `bg-[#6366f1]` (save CTA)
    - src/components/NameSessionModal/NameSessionModal.tsx contains `Skip — use date/time name`
    - src/components/NameSessionModal/NameSessionModal.tsx contains `focus:border-[#6366f1]` (input focus ring)
    - src/components/NameSessionModal/NameSessionModal.tsx does NOT contain `bg-red-600`
    - src/components/NameSessionModal/NameSessionModal.tsx does NOT contain `font-bold` (on h2)
    - src/components/DeleteConfirmModal/DeleteConfirmModal.tsx contains `bg-[#111827]`
    - src/components/DeleteConfirmModal/DeleteConfirmModal.tsx contains `bg-[#ef4444]` (destructive confirm)
    - src/components/DeleteConfirmModal/DeleteConfirmModal.tsx contains `its scorecard. This cannot be undone`
    - src/components/DeleteConfirmModal/DeleteConfirmModal.tsx does NOT contain `bg-red-600`
    - All tests pass (npm test -- --run exits 0)
  </acceptance_criteria>
  <done>NameSessionModal: #111827 panel, 20px radius, indigo CTA, indigo focus ring, "Skip — use date/time name". DeleteConfirmModal: #111827 panel, red-500 destructive confirm, updated copy.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign SpeechSupportBanner with dismiss button</name>
  <read_first>src/components/common/SpeechSupportBanner.tsx</read_first>
  <files>src/components/common/SpeechSupportBanner.tsx</files>
  <action>
Rewrite `src/components/common/SpeechSupportBanner.tsx` completely. This is the one component in Phase 7 that requires a small logic addition: a dismiss button backed by a `useState<boolean>` flag.

Full replacement:
```tsx
// src/components/common/SpeechSupportBanner.tsx
// AUD-05: Non-blocking warning when Web Speech API is unavailable.
import { useState } from 'react';

export default function SpeechSupportBanner() {
  const [dismissed, setDismissed] = useState(false);

  const isSupported =
    'SpeechRecognition' in window || 'webkitSpeechRecognition' in window;

  if (isSupported || dismissed) return null;

  return (
    <div
      role="status"
      className="w-full bg-[#451a03] border-l-[3px] border-amber-400 text-amber-200 text-sm px-4 py-3 rounded-r-lg flex items-start justify-between gap-2"
    >
      <span>Audio analysis requires Chrome or Edge. Eye contact, expressiveness, and gesture analysis will still work.</span>
      <button
        type="button"
        aria-label="Dismiss"
        onClick={() => setDismissed(true)}
        className="text-amber-400 text-[13px] flex-shrink-0 -mx-[14px] -my-[14px] px-[14px] py-[14px] hover:text-amber-200 focus-visible:outline focus-visible:outline-2 focus-visible:outline-amber-400 focus-visible:outline-offset-1"
      >
        ×
      </button>
    </div>
  );
}
```

Key spec points:
- `bg-[#451a03]` (amber-950) background
- `border-l-[3px] border-amber-400` left accent border (3px, not full border)
- `text-amber-200` body text
- `rounded-r-lg` rounds the right side only (left is flat against the border)
- Dismiss button: `text-amber-400`, `×` character, 44px tap target via `-mx-[14px] -my-[14px] px-[14px] py-[14px]` (same pattern as Phase 05-03 marker tap area)
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run --reporter=verbose src/components/common/SpeechSupportBanner.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/common/SpeechSupportBanner.tsx contains `bg-[#451a03]`
    - src/components/common/SpeechSupportBanner.tsx contains `border-l-[3px] border-amber-400`
    - src/components/common/SpeechSupportBanner.tsx contains `text-amber-200`
    - src/components/common/SpeechSupportBanner.tsx contains `useState(false)` (dismissed state)
    - src/components/common/SpeechSupportBanner.tsx contains `setDismissed(true)` (dismiss handler)
    - src/components/common/SpeechSupportBanner.tsx contains `aria-label="Dismiss"`
    - src/components/common/SpeechSupportBanner.tsx does NOT contain `bg-yellow-900`
    - src/components/common/SpeechSupportBanner.tsx does NOT contain `border-yellow-600`
    - SpeechSupportBanner.test.tsx passes (if it exists) — if tests fail due to new dismiss behavior, update tests to cover dismiss click
  </acceptance_criteria>
  <done>SpeechSupportBanner: #451a03 bg, amber-400 3px left border, amber-200 text, dismiss × button with 44px tap area</done>
</task>

</tasks>

<verification>
After both tasks, run full suite and verify all phase plans complete:
- `grep "bg-red-600" src/components/NameSessionModal/NameSessionModal.tsx` returns no match
- `grep "bg-yellow-900" src/components/common/SpeechSupportBanner.tsx` returns no match
- `grep "451a03" src/components/common/SpeechSupportBanner.tsx` returns a match
- `npm test -- --run` exits 0 (full suite green — phase gate)
</verification>

<success_criteria>
- NameSessionModal: #111827 panel, 20px radius, indigo CTA full-width, indigo input focus ring, "Skip — use date/time name" link
- DeleteConfirmModal: #111827 panel, red-500 destructive confirm, correct spec copy
- SpeechSupportBanner: #451a03 bg, amber-400 3px left border, amber-200 text, dismiss button with 44px tap area
- Full test suite green (phase gate)
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-07-SUMMARY.md`
</output>
