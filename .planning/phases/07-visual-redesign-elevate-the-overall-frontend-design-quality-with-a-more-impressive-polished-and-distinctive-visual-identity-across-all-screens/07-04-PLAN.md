---
phase: 07-visual-redesign
plan: 04
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/components/ScorecardView/ScorecardView.tsx
autonomous: true
requirements: [UI-SCORE-01]

must_haves:
  truths:
    - "Overall score is displayed inside a 120px SVG ring with indigo stroke and indigo glow"
    - "Score number inside the ring is display-size (text-5xl/600/tabular-nums)"
    - "Dimension score bars animate from 0% width to score% on mount (300ms ease-out)"
    - "Dimension bar track is 4px tall with #1a2235 background"
    - "Dimension label is 13px/400/slate-400 on left; detail text on right also 13px/slate-400"
    - "Loading state and scorecard card use #111827 surface with border"
  artifacts:
    - path: "src/components/ScorecardView/ScorecardView.tsx"
      provides: "Redesigned scorecard with SVG ring and animated bars"
      contains: "stroke-dasharray"
  key_links:
    - from: "src/components/ScorecardView/ScorecardView.tsx"
      to: "SVG circle element"
      via: "strokeDashoffset computed from score/100 * circumference"
      pattern: "CIRC - .score / 100. \\* CIRC"
---

<objective>
Redesign ScorecardView with two net-new visual elements: (1) an inline SVG score ring replacing the plain `<output>` number, and (2) animated dimension bar widths on mount. This plan also updates the card surface, typography, and bar track per spec.

Purpose: The scorecard is the product's primary value-delivery moment — it must feel premium and visually communicate coaching data, not just show numbers.
Output: `src/components/ScorecardView/ScorecardView.tsx` with SVG ring, animated bars, correct typography, and #111827 card surface.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-UI-SPEC.md
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SVG score ring and update card surface</name>
  <read_first>src/components/ScorecardView/ScorecardView.tsx</read_first>
  <files>src/components/ScorecardView/ScorecardView.tsx</files>
  <action>
Rewrite `src/components/ScorecardView/ScorecardView.tsx` with the following structure. Keep the `scoreBarColor` function, `ScorecardViewProps`, and `DIMENSIONS` array unchanged.

Add these constants before the component:
```tsx
const RADIUS = 54;
const CIRC = 2 * Math.PI * RADIUS; // ≈ 339.3
```

**Loading state div:**
Replace `bg-gray-900 rounded-2xl p-6 w-full max-w-2xl text-gray-400` with `bg-[#111827] border border-[rgba(255,255,255,0.07)] rounded-xl p-6 w-full max-w-2xl text-[#94a3b8]`

**Main card div:**
Replace `bg-gray-900 rounded-2xl p-6 w-full max-w-2xl flex flex-col gap-4` with `bg-[#111827] border border-[rgba(255,255,255,0.07)] rounded-xl p-6 w-full max-w-2xl flex flex-col gap-6`

**Heading h2:**
Replace `text-sm text-gray-400` with `text-sm text-[#94a3b8] tracking-wide`

**Overall score section** — replace the entire `<div className="flex flex-col items-center gap-1">` block with the SVG ring:
```tsx
<div className="flex flex-col items-center gap-3">
  <div className="relative w-[120px] h-[120px]">
    <svg width="120" height="120" viewBox="0 0 120 120" aria-hidden="true">
      {/* Track ring */}
      <circle cx="60" cy="60" r={RADIUS} fill="none" stroke="#1a2235" strokeWidth="6" />
      {/* Score arc — indigo, starts at 12 o'clock via rotate(-90deg) */}
      <circle
        cx="60"
        cy="60"
        r={RADIUS}
        fill="none"
        stroke="#6366f1"
        strokeWidth="6"
        strokeLinecap="round"
        strokeDasharray={CIRC}
        strokeDashoffset={CIRC - (scorecard.overall / 100) * CIRC}
        style={{
          transform: 'rotate(-90deg)',
          transformOrigin: '60px 60px',
          filter: 'drop-shadow(0 0 8px rgba(99,102,241,0.4))',
        }}
      />
    </svg>
    <output
      aria-label="Overall score"
      className="absolute inset-0 flex items-center justify-center text-5xl font-semibold tabular-nums text-[#f1f5f9]"
    >
      {scorecard.overall}
    </output>
  </div>
  <span className="text-sm text-[#94a3b8]">Overall Score</span>
</div>
```
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run --reporter=verbose src/components/ScorecardView/ScorecardView.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ScorecardView/ScorecardView.tsx contains `stroke-dasharray={CIRC}`
    - src/components/ScorecardView/ScorecardView.tsx contains `strokeDashoffset={CIRC - (scorecard.overall / 100) * CIRC}`
    - src/components/ScorecardView/ScorecardView.tsx contains `transformOrigin: '60px 60px'`
    - src/components/ScorecardView/ScorecardView.tsx contains `bg-[#111827] border border-[rgba(255,255,255,0.07)]`
    - src/components/ScorecardView/ScorecardView.tsx contains `text-5xl font-semibold tabular-nums`
    - src/components/ScorecardView/ScorecardView.tsx does NOT contain `bg-gray-900`
    - ScorecardView.test.tsx passes (aria-label "Overall score" still present)
  </acceptance_criteria>
  <done>SVG score ring with indigo stroke, glow, and correct rotation; card surface updated to #111827</done>
</task>

<task type="auto">
  <name>Task 2: Animate dimension bars and update dimension row typography</name>
  <read_first>src/components/ScorecardView/ScorecardView.tsx</read_first>
  <files>src/components/ScorecardView/ScorecardView.tsx</files>
  <action>
Update the dimension rows in `src/components/ScorecardView/ScorecardView.tsx` to use animated bar widths and updated typography. This task runs after Task 1.

**Add import at top of file:**
```tsx
import { useEffect, useState } from 'react';
```

**Add animated state inside the component function** (before the return statement, after the null check):
```tsx
const [animated, setAnimated] = useState(false);
useEffect(() => {
  requestAnimationFrame(() => setAnimated(true));
}, []);
```

**Dimension rows** — replace the entire `DIMENSIONS.map` block with:
```tsx
{DIMENSIONS.map(({ key, label }) => {
  const dim = scorecard.dimensions[key];
  return (
    <div key={key} className="flex flex-col gap-1.5">
      <div className="flex justify-between items-baseline">
        <span className="text-[13px] text-[#94a3b8] tracking-wide">{label}</span>
        <span className="text-[13px] text-[#94a3b8]">{dim.detail ?? 'Insufficient data'}</span>
      </div>
      {/* Bar track: 4px, #1a2235 bg */}
      <div className="w-full h-1 bg-[#1a2235] rounded-full">
        <div
          role="meter"
          aria-label={`${label} score`}
          aria-valuenow={dim.score}
          aria-valuemin={0}
          aria-valuemax={100}
          className={`h-1 rounded-full motion-safe:transition-all motion-safe:duration-300 ${scoreBarColor(dim.score)}`}
          style={{ width: animated ? `${dim.score}%` : '0%' }}
        />
      </div>
    </div>
  );
})}
```

Note: `h-1` is 4px in Tailwind. The `scoreBarColor` function is unchanged — it still returns `bg-emerald-500` / `bg-amber-400` / `bg-red-500` which are correct semantic score colors per Phase 05-02 decision.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ScorecardView/ScorecardView.tsx contains `const [animated, setAnimated] = useState(false)`
    - src/components/ScorecardView/ScorecardView.tsx contains `requestAnimationFrame(() => setAnimated(true))`
    - src/components/ScorecardView/ScorecardView.tsx contains `style={{ width: animated ? \`${dim.score}%\` : '0%' }}`
    - src/components/ScorecardView/ScorecardView.tsx contains `motion-safe:transition-all motion-safe:duration-300`
    - src/components/ScorecardView/ScorecardView.tsx contains `bg-[#1a2235]` (bar track)
    - src/components/ScorecardView/ScorecardView.tsx contains `text-[13px] text-[#94a3b8]` (dimension labels)
    - All tests pass (npm test -- --run exits 0)
  </acceptance_criteria>
  <done>Dimension bars animate from 0% to score% on mount via RAF + useState; 4px track with #1a2235 bg; 13px slate-400 labels</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep "stroke-dasharray" src/components/ScorecardView/ScorecardView.tsx` returns a match
- `grep "bg-gray-900" src/components/ScorecardView/ScorecardView.tsx` returns no match
- `grep "animated" src/components/ScorecardView/ScorecardView.tsx` returns matches for state + useEffect + style
- `npm test -- --run` exits 0
</verification>

<success_criteria>
- SVG score ring: 120px, 6px indigo stroke, drop-shadow glow, rotated to start at 12 o'clock
- Score number inside ring: text-5xl font-semibold tabular-nums
- Dimension bars: animate from 0% on mount, 300ms ease-out, 4px track
- Card surface: #111827 with rgba(255,255,255,0.07) border
- All tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-04-SUMMARY.md`
</output>
