---
phase: 07-visual-redesign
plan: 06
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/pages/HistoryView.tsx
  - src/components/SessionListItem/SessionListItem.tsx
  - src/components/SparklineChart/SparklineChart.tsx
  - src/components/StorageQuotaBar/StorageQuotaBar.tsx
autonomous: true
requirements: [UI-HISTORY-01]

must_haves:
  truths:
    - "HistoryView list header has 'Past Sessions' heading and StorageQuotaBar right-aligned in same row"
    - "Column header row (Session / Date / Duration / Score) is removed"
    - "Session cards use #111827 bg with 1px border that brightens on hover"
    - "Score badge uses inline style with semantic color at 15% opacity bg (no Tailwind dynamic class)"
    - "Sparkline stroke is indigo (#6366f1), no fill"
    - "StorageQuotaBar track is 6px (h-1.5), fill is indigo for normal state"
    - "CTA buttons in HistoryView are indigo, not red"
  artifacts:
    - path: "src/pages/HistoryView.tsx"
      provides: "Redesigned history page"
      contains: "Past Sessions"
    - path: "src/components/SessionListItem/SessionListItem.tsx"
      provides: "Card layout with inline-style score badge"
      contains: "scoreBadgeStyle"
    - path: "src/components/SparklineChart/SparklineChart.tsx"
      provides: "Indigo sparkline"
      contains: "#6366f1"
    - path: "src/components/StorageQuotaBar/StorageQuotaBar.tsx"
      provides: "6px track with indigo fill"
      contains: "h-1.5"
  key_links:
    - from: "src/components/SessionListItem/SessionListItem.tsx"
      to: "scoreBadgeStyle function"
      via: "inline CSSProperties — avoids Tailwind JIT dynamic class issue"
      pattern: "scoreBadgeStyle"
---

<objective>
Redesign the HistoryView page and its four supporting components: SessionListItem, SparklineChart, StorageQuotaBar. Key changes: remove the column header row, add "Past Sessions" + StorageQuotaBar in a flex row header, update card and badge styles, switch sparkline to indigo, and enlarge the StorageQuotaBar track to 6px.

Purpose: History is where users track progress over time — the list and sparklines must feel data-rich and consistent with the new design language.
Output: Four files updated per spec.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-UI-SPEC.md
@.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign HistoryView.tsx + SessionListItem.tsx + SparklineChart.tsx + StorageQuotaBar.tsx</name>
  <read_first>
    src/pages/HistoryView.tsx
    src/components/SessionListItem/SessionListItem.tsx
    src/components/SparklineChart/SparklineChart.tsx
    src/components/StorageQuotaBar/StorageQuotaBar.tsx
  </read_first>
  <files>src/pages/HistoryView.tsx, src/components/SessionListItem/SessionListItem.tsx, src/components/SparklineChart/SparklineChart.tsx, src/components/StorageQuotaBar/StorageQuotaBar.tsx</files>
  <action>
**HistoryView.tsx — changes (keep all logic, useLiveQuery, delete state, modal unchanged):**

1. Loading state div: replace `bg-gray-950 text-gray-400` with `bg-[#080c14] text-[#94a3b8]`

2. Empty state div: replace `bg-gray-950` with `bg-[#080c14]`; replace `text-white gap-4` with `text-[#f1f5f9] gap-4`; update empty state p tag text to `"Record your first practice session to see your history and track your progress."`; replace CTA button `bg-red-600 hover:bg-red-500 font-bold` with `bg-[#6366f1] hover:bg-[#818cf8] font-semibold motion-safe:transition-shadow motion-safe:duration-200 hover:shadow-[0_0_20px_rgba(99,102,241,0.3)]`; change button label from "Record New Session" to "Start Recording"

3. Main page outer div: replace `bg-gray-950` with `bg-[#080c14]`

4. **Remove the column header row** — delete the entire `<div className="w-full max-w-2xl flex items-center gap-4 px-4 text-gray-400 text-sm">` block with its Session/Date/Duration/Score spans

5. **Replace the h1 + CTA button with a new list header row:**
```tsx
<div className="w-full max-w-2xl flex items-center justify-between">
  <h1 className="text-xl font-semibold text-[#f1f5f9]">Past Sessions</h1>
  <button
    onClick={onRecordNew}
    className="px-6 h-[44px] bg-[#6366f1] hover:bg-[#818cf8] text-white font-semibold rounded-xl text-sm
               motion-safe:transition-shadow motion-safe:duration-200
               hover:shadow-[0_0_20px_rgba(99,102,241,0.3)]
               focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#6366f1] focus-visible:outline-offset-2"
  >
    Start Recording
  </button>
</div>
```

6. Sessions list gap: change `gap-2` to `gap-2` (keep 8px gap between cards — correct)

7. Progress section heading: replace `text-sm font-semibold text-gray-400 uppercase tracking-wide` with `text-sm font-semibold text-[#94a3b8] tracking-wide`

8. StorageQuotaBar placement: move `<StorageQuotaBar />` to appear INSIDE the list header row, right-aligned. Replace the standalone `<StorageQuotaBar />` below the sparklines with an update to the list header div to be:
```tsx
<div className="w-full max-w-2xl flex items-center justify-between gap-4">
  <h1 className="text-xl font-semibold text-[#f1f5f9]">Past Sessions</h1>
  <div className="flex items-center gap-4">
    <StorageQuotaBar />
    <button ...>Start Recording</button>
  </div>
</div>
```
Remove the standalone `<StorageQuotaBar />` that was between sparklines and the delete modal.

---

**SessionListItem.tsx — changes:**

1. Replace the `scoreBadgeClass` function entirely with `scoreBadgeStyle` returning `React.CSSProperties`:
```tsx
function scoreBadgeStyle(scorecard: Session['scorecard']): React.CSSProperties {
  if (scorecard === null) return { backgroundColor: 'rgba(148,163,184,0.15)', color: '#94a3b8' };
  const s = scorecard.overall;
  if (s >= 70) return { backgroundColor: 'rgba(16,185,129,0.15)', color: '#10b981' };
  if (s >= 40) return { backgroundColor: 'rgba(251,191,36,0.15)', color: '#fbbf24' };
  return { backgroundColor: 'rgba(239,68,68,0.15)', color: '#ef4444' };
}
```

2. Outer card div: replace `bg-gray-900 rounded-2xl px-4 py-4 flex items-center gap-4 cursor-pointer hover:bg-gray-800 transition-colors` with:
```
bg-[#111827] border border-[rgba(255,255,255,0.07)] rounded-xl px-4 py-4 flex items-center gap-4 cursor-pointer
motion-safe:transition-colors motion-safe:duration-150
hover:bg-[#1a2235] hover:border-[rgba(255,255,255,0.12)]
```

3. Session title span: replace `text-white text-sm font-semibold` with `text-[#f1f5f9] text-base font-semibold`

4. Metadata spans (date, duration): replace `text-gray-400 text-sm` with `text-[#94a3b8] text-[13px]`

5. Score badge span: replace `text-sm font-bold rounded-lg px-2 py-1 ${scoreBadgeClass(session.scorecard)}` with `text-[13px] font-semibold rounded px-2 py-1 tabular-nums` and add `style={scoreBadgeStyle(session.scorecard)}`

6. Delete button: replace `text-gray-600 hover:text-red-400 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-500 transition-colors text-lg leading-none px-2` with `text-[#475569] hover:text-[#ef4444] hover:bg-[rgba(239,68,68,0.1)] motion-safe:transition-colors motion-safe:duration-150 text-[13px] leading-none px-2 py-1 rounded focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#ef4444] focus-visible:outline-offset-1`

---

**SparklineChart.tsx — changes:**

1. SVG `<path>` stroke: replace `stroke="rgb(251 191 36)"` with `stroke="#6366f1"`
2. SVG `<circle>` fill: replace `fill="rgb(251 191 36)"` with `fill="#6366f1"`
3. Remove `opacity="0.9"` from the path (spec says no opacity on indigo sparkline)
4. "Need more sessions" span: replace `text-gray-600` with `text-[#475569]`
5. Label span: replace `text-gray-400` with `text-[#94a3b8]`
6. Trend label classes: keep `text-green-400` / `text-red-400` / `text-gray-400` — these are correct semantic trend indicators

---

**StorageQuotaBar.tsx — changes:**

1. Track div: replace `h-1 bg-gray-800` with `h-1.5 bg-[#1a2235]` (6px track, #1a2235 bg)
2. Fill div: replace `h-1` with `h-1.5`
3. Normal fill: replace `bg-gray-600` (in `fillClass` computed value) with `'bg-[#6366f1]'` for the normal state:
```tsx
const fillClass = isCritical
  ? 'bg-red-500'
  : isWarning
    ? 'bg-amber-400'
    : 'bg-[#6366f1]';
```
4. Label: replace `<span className={labelClass}>Storage used: {usedMB} MB of {totalMB} MB</span>` — move label to appear BELOW the bar track (flex-col, label second):
```tsx
return (
  <div className="flex flex-col gap-1 w-full">
    <div className="w-full h-1.5 bg-[#1a2235] rounded-full">
      <div
        className={`h-1.5 rounded-full motion-safe:transition-all ${fillClass}`}
        style={{ width: `${pct}%` }}
      />
    </div>
    <span className={`text-[13px] tracking-wide ${isCritical ? 'text-red-400' : 'text-[#94a3b8]'}`}>
      {usedMB} MB of {totalMB} MB
    </span>
    {isCritical && (
      <span className="text-red-400 text-[13px]">
        Storage almost full. Delete older sessions to keep recording.
      </span>
    )}
  </div>
);
```
Remove the separate `!isCritical && isWarning` warning message (keep only the critical one — spec does not show an amber warning message, only critical).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/HistoryView.tsx contains `Past Sessions`
    - src/pages/HistoryView.tsx does NOT contain `Session` `Date` `Duration` `Score` column header row
    - src/pages/HistoryView.tsx does NOT contain `bg-red-600`
    - src/pages/HistoryView.tsx contains `bg-[#6366f1]` (CTA)
    - src/pages/HistoryView.tsx contains `bg-[#080c14]`
    - src/components/SessionListItem/SessionListItem.tsx contains `function scoreBadgeStyle`
    - src/components/SessionListItem/SessionListItem.tsx contains `rgba(16,185,129,0.15)` (emerald badge bg)
    - src/components/SessionListItem/SessionListItem.tsx does NOT contain `scoreBadgeClass`
    - src/components/SessionListItem/SessionListItem.tsx contains `bg-[#111827] border border-[rgba(255,255,255,0.07)]`
    - src/components/SparklineChart/SparklineChart.tsx contains `stroke="#6366f1"`
    - src/components/StorageQuotaBar/StorageQuotaBar.tsx contains `h-1.5`
    - src/components/StorageQuotaBar/StorageQuotaBar.tsx contains `bg-[#6366f1]` (normal fill)
    - All tests pass (npm test -- --run exits 0)
  </acceptance_criteria>
  <done>HistoryView: "Past Sessions" header with StorageQuotaBar inline, no column header, indigo CTA. SessionListItem: #111827 card with border hover, inline-style score badge. SparklineChart: indigo stroke. StorageQuotaBar: 6px track, indigo fill, label below.</done>
</task>

</tasks>

<verification>
After task:
- `grep "bg-red-600" src/pages/HistoryView.tsx` returns no match
- `grep "scoreBadgeClass" src/components/SessionListItem/SessionListItem.tsx` returns no match
- `grep "6366f1" src/components/SparklineChart/SparklineChart.tsx` returns a match
- `grep "h-1.5" src/components/StorageQuotaBar/StorageQuotaBar.tsx` returns a match
- `npm test -- --run` exits 0
</verification>

<success_criteria>
- HistoryView: "Past Sessions" heading with inline StorageQuotaBar, no column header row, indigo CTA
- SessionListItem: card layout with hover states, inline-style badge at 15% opacity semantic color
- SparklineChart: indigo (#6366f1) stroke and dots, no fill
- StorageQuotaBar: 6px track, indigo fill for normal state, label below bar
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-redesign-elevate-the-overall-frontend-design-quality-with-a-more-impressive-polished-and-distinctive-visual-identity-across-all-screens/07-06-SUMMARY.md`
</output>
