---
phase: 03-post-session-review
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/ScorecardView/ScorecardView.tsx
  - src/components/ScorecardView/ScorecardView.test.tsx
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02

must_haves:
  truths:
    - "ScorecardView renders the overall score in a large centered Display-size number"
    - "ScorecardView renders exactly 5 per-dimension rows (Eye Contact, Filler Words, Pacing, Expressiveness, Nervous Gestures)"
    - "Each dimension row shows a score bar filled to width proportional to score (0–100 = 0%–100%)"
    - "Each dimension row shows the detail string (e.g. '3 breaks', '~2.1/min')"
    - "Loading state shows 'Calculating scores...' text"
    - "No-data dimension (score=50, detail='Insufficient data') renders bar at 50% width"
  artifacts:
    - path: "src/components/ScorecardView/ScorecardView.tsx"
      provides: "Stateless scorecard display component accepting ScorecardResult prop"
      exports: ["default ScorecardView"]
    - path: "src/components/ScorecardView/ScorecardView.test.tsx"
      provides: "Component tests for SCORE-01 rendering"
      contains: "describe('ScorecardView'"
  key_links:
    - from: "src/components/ScorecardView/ScorecardView.tsx"
      to: "src/analysis/scorer.ts"
      via: "import type { ScorecardResult } from '../../analysis/scorer'"
      pattern: "import.*ScorecardResult.*scorer"
---

<objective>
Build the `ScorecardView` component: a stateless display card that receives a `ScorecardResult` prop and renders the overall score, five dimension rows with score bars, and detail text. Also write component tests verifying correct rendering.

Purpose: This component is the visual output of the scoring layer (plan 03-01). It is composed into `Review.tsx` in plan 03-03. Being stateless and purely prop-driven makes it straightforwardly testable.

Output: `src/components/ScorecardView/ScorecardView.tsx`, `src/components/ScorecardView/ScorecardView.test.tsx`.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-post-session-review/03-UI-SPEC.md
@.planning/phases/03-post-session-review/03-RESEARCH.md
@.planning/phases/03-post-session-review/03-01-SUMMARY.md

<interfaces>
<!-- Types from plan 03-01. Do NOT redefine — import from scorer.ts. -->

From src/analysis/scorer.ts (created in plan 03-01):
```typescript
export interface DimensionScore {
  score: number;     // 0–100
  label: string;     // e.g. "82 / 100"
  detail?: string;   // e.g. "3 breaks"
}

export interface ScorecardResult {
  overall: number;  // 0–100
  dimensions: {
    eyeContact: DimensionScore;
    expressiveness: DimensionScore;
    gestures: DimensionScore;
    fillers: DimensionScore;
    pacing: DimensionScore;
  };
}
```

From src/components/SetupScreen (existing pattern reference — Tailwind v4 dark theme):
- Page background: `bg-gray-950`
- Card background: `bg-gray-900 rounded-2xl p-6`
- Primary text: `text-white`
- Secondary/detail text: `text-gray-400`
- Primary button: `bg-red-600 hover:bg-red-500 rounded-xl`
- Transitions: `transition-colors`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ScorecardView component and tests</name>
  <files>
    src/components/ScorecardView/ScorecardView.tsx
    src/components/ScorecardView/ScorecardView.test.tsx
  </files>
  <read_first>
    - src/analysis/scorer.ts (DimensionScore and ScorecardResult interfaces — import from here)
    - .planning/phases/03-post-session-review/03-UI-SPEC.md (ScorecardView layout, copywriting, color, typography)
    - src/components/SetupScreen/SetupScreen.tsx (existing component — Tailwind v4 dark theme patterns to replicate)
    - .planning/phases/03-post-session-review/03-RESEARCH.md (Anti-patterns section — score bar, no charting library)
  </read_first>
  <behavior>
    Write tests first (RED), then implement until tests pass (GREEN).

    Tests to write in ScorecardView.test.tsx:
    1. Given a full ScorecardResult fixture, renders the overall score number (e.g. "78") in the DOM
    2. Renders exactly 5 dimension name labels: "Eye Contact", "Filler Words", "Pacing", "Expressiveness", "Nervous Gestures"
    3. Each score bar fill div has `style.width` matching `{score}%` for each dimension
    4. Each dimension detail string (e.g. "3 breaks") appears in the DOM
    5. When scorecard prop is null, renders "Calculating scores..." text
    6. Score bar fill has aria-valuenow={score}, aria-valuemin={0}, aria-valuemax={100}

    Use a fixture ScorecardResult for tests:
    ```typescript
    const fixtureScorecard: ScorecardResult = {
      overall: 78,
      dimensions: {
        eyeContact: { score: 82, label: '82 / 100', detail: '3 breaks' },
        fillers: { score: 65, label: '65 / 100', detail: '5 fillers (~1.0/min)' },
        pacing: { score: 100, label: '100 / 100', detail: '130 wpm' },
        expressiveness: { score: 70, label: '70 / 100', detail: '4 segments analyzed' },
        gestures: { score: 84, label: '84 / 100', detail: '2 nervous gestures' },
      },
    };
    ```
  </behavior>
  <action>
    Create `src/components/ScorecardView/` directory if it does not exist.

    --- ScorecardView.test.tsx ---

    Import pattern:
    ```typescript
    import { render, screen } from '@testing-library/react';
    import { describe, it, expect } from 'vitest';
    import ScorecardView from './ScorecardView';
    import type { ScorecardResult } from '../../analysis/scorer';
    ```

    Use `render(<ScorecardView scorecard={fixtureScorecard} />)` and `render(<ScorecardView scorecard={null} />)`.

    For score bar width assertion: query by `aria-label="{dimension} score"` on the fill div, then check `element.style.width === '82%'`.

    --- ScorecardView.tsx ---

    Props interface:
    ```typescript
    interface ScorecardViewProps {
      scorecard: ScorecardResult | null;
    }
    ```

    Loading state (scorecard is null):
    ```tsx
    <div aria-busy="true" className="bg-gray-900 rounded-2xl p-6 w-full max-w-2xl text-gray-400">
      Calculating scores...
    </div>
    ```

    Normal state layout (from UI-SPEC ScorecardView section):
    ```tsx
    <div className="bg-gray-900 rounded-2xl p-6 w-full max-w-2xl flex flex-col gap-4">
      {/* Heading */}
      <h2 className="text-sm text-gray-400">Your Session Scorecard</h2>

      {/* Overall score — Display size (text-3xl), bold */}
      <div className="flex flex-col items-center gap-1">
        <output aria-label="Overall score" className="text-3xl font-bold text-white">
          {scorecard.overall}
        </output>
        <span className="text-sm text-gray-400">Overall Score</span>
      </div>

      {/* Five dimension rows */}
      {DIMENSIONS.map(({ key, label }) => {
        const dim = scorecard.dimensions[key];
        return (
          <div key={key} className="flex flex-col gap-1">
            <div className="flex justify-between items-baseline">
              <span className="text-sm text-white">{label}</span>
              <span className="text-sm text-gray-400">{dim.detail ?? 'Insufficient data'}</span>
            </div>
            {/* Score bar: bg-gray-700 track, bg-red-600 fill */}
            <div className="w-full h-2 bg-gray-700 rounded-full">
              <div
                role="meter"
                aria-label={`${label} score`}
                aria-valuenow={dim.score}
                aria-valuemin={0}
                aria-valuemax={100}
                className="h-2 bg-red-600 rounded-full"
                style={{ width: `${dim.score}%` }}
              />
            </div>
          </div>
        );
      })}
    </div>
    ```

    DIMENSIONS constant (module-level, maps DimensionScore keys to display labels per UI-SPEC Copywriting Contract):
    ```typescript
    const DIMENSIONS: { key: keyof ScorecardResult['dimensions']; label: string }[] = [
      { key: 'eyeContact', label: 'Eye Contact' },
      { key: 'fillers', label: 'Filler Words' },
      { key: 'pacing', label: 'Pacing' },
      { key: 'expressiveness', label: 'Expressiveness' },
      { key: 'gestures', label: 'Nervous Gestures' },
    ];
    ```

    After writing tests, run them (expect failures), then implement the component, then run again to confirm green.
  </action>
  <verify>
    <automated>npx vitest run src/components/ScorecardView/ScorecardView.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    All ScorecardView tests pass (exit code 0).
    grep -n "Calculating scores" src/components/ScorecardView/ScorecardView.tsx confirms loading state text.
    grep -n "Nervous Gestures" src/components/ScorecardView/ScorecardView.tsx confirms correct dimension label.
    grep -n "role=\"meter\"" src/components/ScorecardView/ScorecardView.tsx confirms accessibility attribute.
    npx vitest run exits 0 (full suite still green).
  </done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose

Expected: all prior tests plus ScorecardView tests green. No regressions in Phase 1/2 test suites.
</verification>

<success_criteria>
- src/components/ScorecardView/ScorecardView.tsx renders 5 dimension rows with score bars, overall score, and loading state
- Dimension labels match UI-SPEC Copywriting Contract exactly: "Eye Contact", "Filler Words", "Pacing", "Expressiveness", "Nervous Gestures"
- Score bar fill elements have role="meter" and aria-valuenow={score}
- Overall score wrapped in output element with aria-label="Overall score"
- All ScorecardView tests pass
- npx vitest run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-post-session-review/03-02-SUMMARY.md`
</output>
