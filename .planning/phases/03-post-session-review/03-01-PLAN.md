---
phase: 03-post-session-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analysis/scorer.ts
  - src/analysis/__tests__/aggregateScores.test.ts
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02

must_haves:
  truths:
    - "aggregateScores(eventLog, durationMs) returns a ScorecardResult with five per-dimension DimensionScore objects and a weighted overall number"
    - "All five scoring functions return correct scores for known fixture inputs (eye contact ratio, filler rate, WPM, gesture count, expressiveness average)"
    - "aggregateScores returns overall = 0 when all dimension scores are 0"
    - "Scorecard can be persisted to Dexie as { overall: number, dimensions: Record<string, number> } matching the existing Scorecard interface in db.ts"
    - "All scorer unit tests pass with npx vitest run src/analysis/__tests__/aggregateScores.test.ts"
  artifacts:
    - path: "src/analysis/scorer.ts"
      provides: "aggregateScores pure function plus DimensionScore and ScorecardResult types"
      exports: ["aggregateScores", "DimensionScore", "ScorecardResult"]
    - path: "src/analysis/__tests__/aggregateScores.test.ts"
      provides: "Unit tests covering SCORE-01 and SCORE-02 with fixture event arrays"
      contains: "describe('aggregateScores'"
  key_links:
    - from: "src/analysis/scorer.ts"
      to: "src/db/db.ts"
      via: "import type { SessionEvent } from '../db/db'"
      pattern: "import.*SessionEvent.*from.*db"
    - from: "src/pages/Review.tsx (Phase 03-03)"
      to: "src/analysis/scorer.ts"
      via: "import { aggregateScores } from '../analysis/scorer'"
      pattern: "aggregateScores"
---

<objective>
Create the score aggregation layer: a pure `aggregateScores` function that walks the session event log and produces per-dimension scores (0–100) and a weighted overall score. Also create the Wave 0 test file with full unit tests for all scoring logic.

Purpose: This is the data foundation for Phase 3. Scorecard display (plan 03-02) and Review page wiring (plan 03-03) both consume the types and function defined here. Implementing and verifying it first ensures downstream plans have a stable, tested contract.

Output: `src/analysis/scorer.ts` (aggregator + types), `src/analysis/__tests__/aggregateScores.test.ts` (unit tests for SCORE-01 + SCORE-02).
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-post-session-review/03-RESEARCH.md
@.planning/phases/03-post-session-review/03-VALIDATION.md

<interfaces>
<!-- Key types the executor must conform to. Do NOT redefine these — import them. -->

From src/db/db.ts:
```typescript
export interface SessionEvent {
  type: string;
  timestampMs: number;
  label?: string;
}

export interface Scorecard {
  overall: number;                       // 0–100
  dimensions: Record<string, number>;    // e.g. { eyeContact: 82, fillers: 65 }
}

export interface Session {
  id: number;
  title: string;
  createdAt: Date;
  durationMs: number;
  videoBlob: Blob;
  eventLog: SessionEvent[];
  scorecard: Scorecard | null;
}
```

Phase 2 event types present in eventLog:
- `eye_contact_break` — gaze goes off-camera
- `eye_contact_resume` — gaze returns to camera
- `face_touch` — hand near face
- `body_sway` — lateral shoulder displacement
- `filler_word` — with `label` = the word (e.g. "um", "uh", "like", "you know")
- `pause_detected` — with `label` = pause duration string
- `expressiveness_segment` — with `label` = stringified 0–1 score (e.g. "0.72")
- `wpm_snapshot` — with `label` = WPM integer string (e.g. "134"), placed at durationMs position
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 test stubs for aggregateScores</name>
  <files>src/analysis/__tests__/aggregateScores.test.ts</files>
  <read_first>
    - src/db/db.ts (SessionEvent type — the fixture event arrays must match this interface exactly)
    - src/analysis/eyeContact.test.ts (test pattern reference — fixture helper style, describe/it/expect structure)
    - .planning/phases/03-post-session-review/03-RESEARCH.md (scoring formulas and expected values)
  </read_first>
  <behavior>
    Write FAILING tests first. scorer.ts does not exist yet — tests must fail with "Cannot find module" or similar before scorer.ts is created.

    Test cases to implement (all use fixture SessionEvent arrays):

    scoreEyeContact cases:
    - 3000ms away in a 60000ms session → ratio = 0.95, score = 95
    - 0ms away (no break events) in 60000ms → score = 100
    - session ended while in break (no resume): 10000ms break to 60000ms = 50000ms away → ratio = 1-50000/60000 = 0.167, score = 17
    - durationMs = 0 → score = 50 (no-data guard)

    scoreFillers cases:
    - 5 filler events in 60000ms (1 min) → fillersPerMin = 5, score = Math.max(0, round(100 - (5/6)*100)) = 17
    - 0 filler events → score = 100
    - 12 filler events in 60000ms → fillersPerMin = 12, score = 0 (floor)

    scorePacing cases:
    - wpm_snapshot label="130" → score = 100 (in 120–160 range)
    - wpm_snapshot label="80" → score = Math.max(0, round(((80-60)/60)*100)) = 33
    - wpm_snapshot label="190" → score = Math.max(0, round(((220-190)/60)*100)) = 50
    - no wpm_snapshot event → score = 50

    scoreExpressiveness cases:
    - three segments with labels "0.8", "0.6", "1.0" → avg = 0.8, score = 80
    - no segments → score = 50

    scoreGestures cases:
    - 3 nervous events → score = 100 - 3*8 = 76
    - 13 or more events → score = 0 (floor)

    aggregateScores overall cases:
    - all dimension scores = 100 → overall = 100
    - eyeContact=80, fillers=60, pacing=100, expressiveness=50, gestures=40 →
      overall = round(80*0.25 + 60*0.25 + 100*0.20 + 50*0.15 + 40*0.15)
             = round(20 + 15 + 20 + 7.5 + 6) = round(68.5) = 69
  </behavior>
  <action>
    Create `src/analysis/__tests__/aggregateScores.test.ts`. The directory `__tests__` may not exist — create it.

    File structure:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { aggregateScores } from '../scorer';
    import type { SessionEvent } from '../../db/db';
    ```

    Write one `describe` block per scoring dimension plus one for `aggregateScores` overall. Use plain `SessionEvent[]` arrays as fixtures — no helper functions needed since events are simple objects.

    The import from `'../scorer'` will fail until scorer.ts is created. Run the test immediately after writing to confirm it fails with a module resolution error (RED phase). Do NOT create scorer.ts yet.
  </action>
  <verify>
    <automated>npx vitest run src/analysis/__tests__/aggregateScores.test.ts 2>&1 | head -20</automated>
  </verify>
  <done>Test file exists and fails with "Cannot find module '../scorer'" or equivalent import error. All test cases listed in behavior block are present in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scorer.ts (GREEN phase)</name>
  <files>src/analysis/scorer.ts</files>
  <read_first>
    - src/analysis/__tests__/aggregateScores.test.ts (the failing tests — implement against these exactly)
    - src/db/db.ts (SessionEvent and Scorecard interfaces)
    - .planning/phases/03-post-session-review/03-RESEARCH.md (full scoring formulas in Pattern 1 — copy verbatim)
  </read_first>
  <behavior>
    Implement until all tests in aggregateScores.test.ts pass.

    Verification of formula correctness:
    - scoreEyeContact: awayMs / durationMs gives fraction away; ratio = 1 - awayMs/durationMs; score = round(ratio * 100)
    - scoreFillers: fillersPerMin = count / (durationMs / 60000); score = max(0, round(100 - (fillersPerMin/6)*100))
    - scorePacing: 120–160 → 100; below 120: max(0, round(((wpm-60)/60)*100)); above 160: max(0, round(((220-wpm)/60)*100))
    - scoreExpressiveness: avg of parseFloat(e.label) across expressiveness_segment events; score = min(100, round(avg*100))
    - scoreGestures: score = max(0, 100 - count * 8) where count = face_touch + body_sway events
    - WEIGHTS: eyeContact=0.25, fillers=0.25, pacing=0.20, expressiveness=0.15, gestures=0.15
  </behavior>
  <action>
    Create `src/analysis/scorer.ts` with the exact content from RESEARCH.md Pattern 1.

    Required exports:
    ```typescript
    export interface DimensionScore {
      score: number;     // 0–100
      label: string;     // e.g. "82 / 100"
      detail?: string;   // e.g. "3 breaks in 5 min"
    }

    export interface ScorecardResult {
      overall: number;
      dimensions: {
        eyeContact: DimensionScore;
        expressiveness: DimensionScore;
        gestures: DimensionScore;
        fillers: DimensionScore;
        pacing: DimensionScore;
      };
    }

    export function aggregateScores(
      eventLog: SessionEvent[],
      durationMs: number
    ): ScorecardResult
    ```

    Internal helper functions (NOT exported — only used by aggregateScores):
    - `scoreEyeContact(events, durationMs): DimensionScore`
    - `scoreExpressiveness(events): DimensionScore`
    - `scoreGestures(events): DimensionScore`
    - `scoreFillers(events, durationMs): DimensionScore`
    - `scorePacing(events): DimensionScore`

    WEIGHTS constant (module-level, NOT exported):
    ```typescript
    const WEIGHTS = {
      eyeContact: 0.25,
      fillers: 0.25,
      pacing: 0.20,
      expressiveness: 0.15,
      gestures: 0.15,
    };
    ```

    Eye contact time calculation (copy exactly from RESEARCH.md — handles break/resume pairs and open-ended breaks):
    Walk events filtered to `eye_contact_break | eye_contact_resume`, sorted by timestampMs. Track `inBreak` boolean and `breakStart`. If session ends while inBreak: `awayMs += durationMs - breakStart`.

    After implementing, run `npx vitest run src/analysis/__tests__/aggregateScores.test.ts` and fix any failures before proceeding.
  </action>
  <verify>
    <automated>npx vitest run src/analysis/__tests__/aggregateScores.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    All tests in aggregateScores.test.ts pass (exit code 0).
    src/analysis/scorer.ts exports `aggregateScores`, `DimensionScore`, and `ScorecardResult`.
    Running `grep -n "export" src/analysis/scorer.ts` shows at least: `export interface DimensionScore`, `export interface ScorecardResult`, `export function aggregateScores`.
  </done>
</task>

</tasks>

<verification>
npx vitest run src/analysis/__tests__/aggregateScores.test.ts --reporter=verbose

Expected: all tests green, covering scoreEyeContact (4 cases), scoreFillers (3 cases), scorePacing (4 cases), scoreExpressiveness (2 cases), scoreGestures (2 cases), aggregateScores overall (2 cases).
</verification>

<success_criteria>
- src/analysis/scorer.ts exists and exports `aggregateScores`, `DimensionScore`, `ScorecardResult`
- All aggregateScores unit tests pass
- `aggregateScores([], 0).overall` returns 50 (graceful no-data default)
- The ScorecardResult.dimensions shape can be mapped to Scorecard.dimensions (Record<string, number>) by taking `.score` from each DimensionScore — no schema mismatch
- npx vitest run exits 0 (full suite still green)
</success_criteria>

<output>
After completion, create `.planning/phases/03-post-session-review/03-01-SUMMARY.md`
</output>
