---
phase: 05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app
plan: 05
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
  - "05-04"
files_modified:
  - src/App.tsx
  - src/components/AnnotatedPlayer/AnnotatedPlayer.tsx
  - src/components/AnnotatedPlayer/AnnotatedPlayer.test.tsx
  - src/components/NameSessionModal/NameSessionModal.tsx
  - src/components/StorageQuotaBar/StorageQuotaBar.tsx
  - src/components/SparklineChart/SparklineChart.tsx
  - src/components/common/SpeechSupportBanner.tsx
  - src/pages/Review.tsx
autonomous: true
requirements:
  - A-05
  - A-09
  - A-10

must_haves:
  truths:
    - "Processing view in App.tsx shows an animate-spin SVG spinner above 'Processing your recording...' text"
    - "AnnotatedPlayer video is wrapped in a relative+group div with an absolutely-positioned overlay button"
    - "Overlay button shows play/pause SVG icon on hover, has aria-label that tracks isPlaying state"
    - "NameSessionModal has no border on the inner dialog div"
    - "NameSessionModal input uses focus:border-red-600 (not red-500)"
    - "NameSessionModal buttons say 'Save Session' and 'Use auto name'"
    - "StorageQuotaBar shows updated critical and warning copy strings"
    - "SparklineChart insufficient-data text says 'Need more sessions'"
    - "SpeechSupportBanner uses text-yellow-600 (not text-yellow-200)"
    - "Review.tsx error message says 'Could not load this session. Try recording a new one.'"
    - "All tests pass"
  artifacts:
    - path: "src/App.tsx"
      provides: "Animated processing spinner (A-05)"
      contains: "animate-spin"
    - path: "src/components/AnnotatedPlayer/AnnotatedPlayer.tsx"
      provides: "Play/pause overlay affordance (A-09)"
      contains: "isPlaying"
    - path: "src/components/NameSessionModal/NameSessionModal.tsx"
      provides: "Cleaned-up modal (A-10), updated copy"
      contains: "Save Session"
    - path: "src/components/StorageQuotaBar/StorageQuotaBar.tsx"
      provides: "Updated copy strings"
      contains: "Storage almost full"
    - path: "src/components/SparklineChart/SparklineChart.tsx"
      provides: "Shorter insufficient-data copy"
      contains: "Need more sessions"
    - path: "src/components/common/SpeechSupportBanner.tsx"
      provides: "Warning color token consolidation"
      contains: "text-yellow-600"
    - path: "src/pages/Review.tsx"
      provides: "Updated error copy"
      contains: "Could not load this session"
  key_links:
    - from: "AnnotatedPlayer.tsx"
      to: "isPlaying state"
      via: "useState<boolean>, onPlay/onPause video events"
      pattern: "isPlaying"
    - from: "AnnotatedPlayer overlay button"
      to: "aria-label"
      via: "aria-label={isPlaying ? 'Pause' : 'Play'}"
      pattern: "isPlaying.*Pause.*Play"
---

<objective>
Fix A-05 (processing spinner), A-09 (play/pause overlay), A-10 (modal cleanup), plus the full copywriting sweep across all components. This is the final plan — after this, all 12 audit findings are resolved.

Purpose: Complete the visual polish phase — add loading affordance, video interaction cue, modal cleanup, and ensure all copy strings match the approved Copywriting Contract.
Output: 8 modified files. All 12 audit findings resolved. Full test suite green.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-UI-SPEC.md
@.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-RESEARCH.md

<interfaces>
<!-- App.tsx processing view (current lines 145-151): -->
<!-- <div className="flex items-center justify-center min-h-screen bg-gray-950 text-gray-400"> -->
<!--   <p>Processing recording...</p>                                                         -->
<!-- </div>                                                                                   -->

<!-- AnnotatedPlayer.tsx current state: -->
<!-- const [currentTimeMs, setCurrentTimeMs] = useState(0);     ← add isPlaying state here -->
<!-- const [progressPct, setProgressPct] = useState(0);                                      -->
<!-- <video ref={videoRef} src={videoUrl}                                                     -->
<!--   onTimeUpdate={handleTimeUpdate} onClick={handleVideoClick}                            -->
<!--   className="w-full max-w-2xl rounded-xl bg-black cursor-pointer"                       -->
<!--   aria-label="Session playback" />                                                       -->

<!-- AnnotatedPlayer.test.tsx existing test that MUST NOT break: -->
<!-- it('video element has aria-label="Session playback"') — aria-label stays on the video    -->
<!-- it('highlights the nearest event marker...') — still works after wrapping video in div   -->

<!-- PITFALL (from RESEARCH.md): -->
<!-- Do NOT attach onClick to <video> AND the overlay button — causes double-toggle.          -->
<!-- Remove onClick from <video> element; attach handleVideoClick ONLY to overlay button.    -->

<!-- NameSessionModal current inner div: -->
<!-- className="bg-gray-900 border border-gray-700 rounded-2xl p-8 w-full max-w-md flex flex-col gap-6 shadow-2xl" -->
<!-- → REMOVE "border border-gray-700"                                                        -->
<!-- Input focus ring: focus:border-red-500 → focus:border-red-600                           -->

<!-- StorageQuotaBar current copy strings: -->
<!-- "Storage is almost full. Delete sessions to avoid losing new recordings." → "Storage almost full. Delete older sessions to keep recording." -->
<!-- "Storage is nearly full. Consider deleting older sessions." → "Storage getting full. Consider deleting older sessions." -->

<!-- SparklineChart current insufficient-data text: -->
<!-- "Record more sessions to see trends" → "Need more sessions"                              -->

<!-- SpeechSupportBanner current classes: -->
<!-- className="w-full bg-yellow-900/60 border border-yellow-600 text-yellow-200 text-sm px-4 py-2 rounded-lg" -->
<!-- → change text-yellow-200 to text-yellow-600, keep bg-yellow-900/60                      -->

<!-- Review.tsx current error string in catch block: -->
<!-- 'Could not load session. Try recording again.' → 'Could not load this session. Try recording a new one.' -->
<!-- NOTE: Plan 05-04 already edited Review.tsx (heading + max-width). Read the file fresh    -->
<!-- before editing — the h1 and outer container will already reflect 05-04's changes.        -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Processing spinner (A-05), play/pause overlay (A-09), NameSessionModal cleanup (A-10)</name>
  <files>src/App.tsx, src/components/AnnotatedPlayer/AnnotatedPlayer.tsx, src/components/AnnotatedPlayer/AnnotatedPlayer.test.tsx, src/components/NameSessionModal/NameSessionModal.tsx</files>
  <read_first>
    - src/App.tsx (MUST read before editing — find lines 145-151, the processing view block)
    - src/components/AnnotatedPlayer/AnnotatedPlayer.tsx (MUST read before editing)
    - src/components/AnnotatedPlayer/AnnotatedPlayer.test.tsx (MUST read before editing — understand what tests exist before modifying component)
    - src/components/NameSessionModal/NameSessionModal.tsx (MUST read before editing)
    - src/components/NameSessionModal/NameSessionModal.test.tsx (existing tests — verify none break)
  </read_first>
  <action>
**App.tsx changes — processing view (A-05):**

Find the processing view block (currently `if (view === 'processing') { return ( <div ...> <p>Processing recording...</p> </div> ); }`)

Replace with:
```tsx
if (view === 'processing') {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen bg-gray-950 gap-4">
      <svg
        className="animate-spin w-8 h-8 text-gray-400"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        aria-hidden="true"
      >
        <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
        <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z" />
      </svg>
      <p className="text-sm text-gray-400">Processing your recording...</p>
    </div>
  );
}
```

Note: No new file is needed. The processing view is inlined in App.tsx (per RESEARCH.md Pitfall 6 — do not create a ProcessingSpinner component).

**AnnotatedPlayer.tsx changes — play/pause overlay (A-09):**

1. Add `isPlaying` state after the existing state declarations:
```tsx
const [isPlaying, setIsPlaying] = useState(false);
```

2. Replace the `<video>` element with a wrapped version:
   - Wrap video in `<div className="relative group w-full max-w-2xl">`
   - Remove `onClick={handleVideoClick}` from the `<video>` element (CRITICAL: prevents double-toggle)
   - Add `onPlay={() => setIsPlaying(true)}` and `onPause={() => setIsPlaying(false)}` to the `<video>` element
   - Remove `max-w-2xl` from the video className (it's now on the wrapper div)
   - Add the overlay button after the video element, before closing the wrapper div

Full replacement for the video section:
```tsx
<div className="relative group w-full max-w-2xl">
  <video
    ref={videoRef}
    src={videoUrl}
    onTimeUpdate={handleTimeUpdate}
    onPlay={() => setIsPlaying(true)}
    onPause={() => setIsPlaying(false)}
    className="w-full rounded-xl bg-black cursor-pointer"
    aria-label="Session playback"
  />
  <button
    onClick={handleVideoClick}
    aria-label={isPlaying ? "Pause" : "Play"}
    className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-150"
  >
    {isPlaying ? (
      <svg className="w-12 h-12 text-white drop-shadow-lg" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <rect x="6" y="4" width="4" height="16" rx="1" />
        <rect x="14" y="4" width="4" height="16" rx="1" />
      </svg>
    ) : (
      <svg className="w-12 h-12 text-white drop-shadow-lg" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <path d="M8 5v14l11-7z" />
      </svg>
    )}
  </button>
</div>
```

The outer `<div className="flex flex-col gap-2 w-full max-w-2xl">` wrapping the whole component stays unchanged.

**AnnotatedPlayer.test.tsx changes:**

The existing test `it('video element has aria-label="Session playback"')` still works — aria-label is still on the video.

The test `it('renders a video element with src equal to the videoUrl prop')` still works — video is still in the DOM.

No test changes needed unless a test checks the exact className of the video element. Read the test file first — if any test uses `videoEl.className` to check for `cursor-pointer`, that test still passes (cursor-pointer is still on video). No test changes required.

**NameSessionModal.tsx changes — A-10 and copywriting:**

1. Inner dialog div: remove `border border-gray-700` from className:
   - From: `className="bg-gray-900 border border-gray-700 rounded-2xl p-8 w-full max-w-md flex flex-col gap-6 shadow-2xl"`
   - To: `className="bg-gray-900 rounded-2xl p-8 w-full max-w-md flex flex-col gap-6 shadow-2xl"`

2. Input focus ring: change `focus:border-red-500` to `focus:border-red-600`:
   - From: `... focus:border-red-500 transition-colors ...`
   - To: `... focus:border-red-600 transition-colors ...`

3. Skip button text: change `Skip` to `Use auto name`:
   - From: `>Skip</button>`
   - To: `>Use auto name</button>`

4. Save button text: change `Save` to `Save Session`:
   - From: `>Save</button>`
   - To: `>Save Session</button>`
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run "AnnotatedPlayer|NameSessionModal" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "animate-spin" src/App.tsx returns a match
    - grep -n "Processing your recording..." src/App.tsx returns a match
    - grep -n "Processing recording..." src/App.tsx returns NO match (old copy replaced)
    - grep -n "isPlaying" src/components/AnnotatedPlayer/AnnotatedPlayer.tsx returns at least 3 matches (useState declaration, onPlay handler, aria-label usage)
    - grep -n 'aria-label={isPlaying' src/components/AnnotatedPlayer/AnnotatedPlayer.tsx returns a match
    - grep -n "group-hover:opacity-100" src/components/AnnotatedPlayer/AnnotatedPlayer.tsx returns a match
    - grep -n "onPlay" src/components/AnnotatedPlayer/AnnotatedPlayer.tsx returns a match
    - grep -n "onPause" src/components/AnnotatedPlayer/AnnotatedPlayer.tsx returns a match
    - grep -n "border border-gray-700" src/components/NameSessionModal/NameSessionModal.tsx returns NO match
    - grep -n "focus:border-red-600" src/components/NameSessionModal/NameSessionModal.tsx returns a match
    - grep -n "focus:border-red-500" src/components/NameSessionModal/NameSessionModal.tsx returns NO match
    - grep -n "Save Session" src/components/NameSessionModal/NameSessionModal.tsx returns a match
    - grep -n "Use auto name" src/components/NameSessionModal/NameSessionModal.tsx returns a match
    - npm test -- --run AnnotatedPlayer exits 0
    - npm test -- --run NameSessionModal exits 0
  </acceptance_criteria>
  <done>Processing view has animated spinner. AnnotatedPlayer has play/pause overlay with state-tracked aria-label. NameSessionModal has no border, correct focus ring color, updated button copy. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Copywriting sweep — StorageQuotaBar, SparklineChart, SpeechSupportBanner, Review error</name>
  <files>src/components/StorageQuotaBar/StorageQuotaBar.tsx, src/components/SparklineChart/SparklineChart.tsx, src/components/common/SpeechSupportBanner.tsx, src/pages/Review.tsx</files>
  <read_first>
    - src/components/StorageQuotaBar/StorageQuotaBar.tsx (MUST read before editing)
    - src/components/SparklineChart/SparklineChart.tsx (MUST read before editing)
    - src/components/common/SpeechSupportBanner.tsx (MUST read before editing)
    - src/pages/Review.tsx (MUST read before editing — will already have 05-04's heading + max-width changes applied)
    - src/components/StorageQuotaBar/StorageQuotaBar.test.tsx (new tests from Plan 01 — copy string tests must now pass)
    - src/components/SparklineChart/SparklineChart.test.tsx (existing tests — verify none check the insufficient-data string)
    - src/pages/Review.test.tsx (existing tests — verify none check the error string)
  </read_first>
  <action>
**StorageQuotaBar.tsx copy changes:**

1. Critical warning copy: change from:
   `"Storage is almost full. Delete sessions to avoid losing new recordings."`
   To:
   `"Storage almost full. Delete older sessions to keep recording."`

2. Warning copy: change from:
   `"Storage is nearly full. Consider deleting older sessions."`
   To:
   `"Storage getting full. Consider deleting older sessions."`

No other changes to StorageQuotaBar (the bg-red-5 fill color was already changed in Plan 02).

**SparklineChart.tsx copy change:**

Change the insufficient-data text from:
`"Record more sessions to see trends"`
To:
`"Need more sessions"`

This is the string inside:
```tsx
<span className="text-xs text-gray-600">Record more sessions to see trends</span>
```
Becomes:
```tsx
<span className="text-xs text-gray-600">Need more sessions</span>
```

**SpeechSupportBanner.tsx warning color change:**

Change `text-yellow-200` to `text-yellow-600` in the div className. Keep `bg-yellow-900/60` and `border-yellow-600` unchanged.

From:
`className="w-full bg-yellow-900/60 border border-yellow-600 text-yellow-200 text-sm px-4 py-2 rounded-lg"`

To:
`className="w-full bg-yellow-900/60 border border-yellow-600 text-yellow-600 text-sm px-4 py-2 rounded-lg"`

**Review.tsx error copy change:**

In the `.catch()` handler, change the error string from:
`'Could not load session. Try recording again.'`
To:
`'Could not load this session. Try recording a new one.'`

This is the string passed to `setError(...)` in the catch block.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run "StorageQuotaBar|SparklineChart|Review" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "Storage almost full. Delete older sessions to keep recording." src/components/StorageQuotaBar/StorageQuotaBar.tsx returns a match
    - grep -n "Storage is almost full" src/components/StorageQuotaBar/StorageQuotaBar.tsx returns NO match (old copy removed)
    - grep -n "Storage getting full. Consider deleting older sessions." src/components/StorageQuotaBar/StorageQuotaBar.tsx returns a match
    - grep -n "Storage is nearly full" src/components/StorageQuotaBar/StorageQuotaBar.tsx returns NO match (old copy removed)
    - grep -n "Need more sessions" src/components/SparklineChart/SparklineChart.tsx returns a match
    - grep -n "Record more sessions to see trends" src/components/SparklineChart/SparklineChart.tsx returns NO match
    - grep -n "text-yellow-600" src/components/common/SpeechSupportBanner.tsx returns a match (at least 2 — border and text)
    - grep -n "text-yellow-200" src/components/common/SpeechSupportBanner.tsx returns NO match
    - grep -n "Could not load this session. Try recording a new one." src/pages/Review.tsx returns a match
    - grep -n "Could not load session. Try recording again." src/pages/Review.tsx returns NO match
    - npm test -- --run StorageQuotaBar exits 0 (copy string tests from Plan 01 now pass)
    - npm test -- --run SparklineChart exits 0
    - npm test -- --run Review exits 0
  </acceptance_criteria>
  <done>StorageQuotaBar shows shortened critical and warning copy. SparklineChart shows "Need more sessions". SpeechSupportBanner uses text-yellow-600. Review.tsx error message updated. All tests pass.</done>
</task>

</tasks>

<verification>
Run the full test suite as the final phase gate:

```bash
cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1
```

Expected: All tests pass. This is the final plan — all 12 audit findings (A-01 through A-12) are now resolved.

Manual verification also required (post-test):
1. Open app in browser, hover over video player — overlay icon should appear
2. Check processing view renders spinner (requires recording a session)
3. Verify View History button in SetupScreen shows "→ View History" with no underline
</verification>

<success_criteria>
- A-05: Processing view has animate-spin SVG spinner and "Processing your recording..." copy
- A-09: Video player shows play/pause overlay icon on hover with correct aria-label
- A-10: NameSessionModal inner div has no border, focus ring uses red-600, buttons say "Save Session" / "Use auto name"
- Copywriting: All 5 copy strings updated per Copywriting Contract
- SpeechSupportBanner: text-yellow-600 (not text-yellow-200)
- Full test suite green
- All 12 audit findings A-01 through A-12 resolved across all 5 plans
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-05-SUMMARY.md`
</output>
