---
phase: 05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app
plan: 04
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - src/pages/Home.tsx
  - src/components/SetupScreen/SetupScreen.tsx
  - src/pages/Review.tsx
  - src/pages/HistoryView.tsx
autonomous: true
requirements:
  - A-04
  - A-06
  - A-11
  - A-12

must_haves:
  truths:
    - "All four page h1 elements use text-xl font-semibold (no page has text-2xl, text-3xl, or text-4xl on an h1)"
    - "Home.tsx outer container has max-w-3xl mx-auto w-full"
    - "Review.tsx outer container has max-w-3xl mx-auto w-full"
    - "HistoryView.tsx outer container has max-w-3xl mx-auto w-full"
    - "Home.tsx footnote uses text-gray-500 (not text-gray-600)"
    - "SetupScreen View History button shows arrow character and has no underline class"
    - "View History button has px-4 py-2 padding for 44px touch target"
    - "Home.test.tsx tests pass for heading class, footnote class, max-width"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "Unified heading (A-04), max-width (A-11), footnote contrast (A-12)"
      contains: "text-xl font-semibold"
    - path: "src/components/SetupScreen/SetupScreen.tsx"
      provides: "View History affordance (A-06), heading (A-04)"
      contains: "→ View History"
    - path: "src/pages/Review.tsx"
      provides: "Unified heading (A-04), max-width (A-11)"
      contains: "text-xl font-semibold"
    - path: "src/pages/HistoryView.tsx"
      provides: "Unified heading (A-04), max-width (A-11)"
      contains: "text-xl font-semibold"
  key_links:
    - from: "Home.tsx"
      to: "outer container"
      via: "max-w-3xl mx-auto w-full class on root div"
      pattern: "max-w-3xl"
    - from: "SetupScreen.tsx"
      to: "View History button"
      via: "→ arrow + no underline + px-4 py-2"
      pattern: "→ View History"
---

<objective>
Fix A-04 (heading hierarchy — all h1 to text-xl font-semibold), A-06 (View History navigation affordance), A-11 (max-width on page containers), and A-12 (Home footnote contrast). These are all pure Tailwind class changes and one copy change.

Purpose: Resolve the typography inconsistency (four different h1 sizes across four pages) and ensure content is readable on wide displays with a max-width constraint.
Output: 4 modified page/component files with consistent heading scale, max-width constraints, and improved navigation affordance.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-UI-SPEC.md
@.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-RESEARCH.md

<interfaces>
<!-- Current h1 values per file (from source inspection): -->
<!-- Home.tsx:        className="text-4xl font-bold tracking-tight"   → CHANGE TO text-xl font-semibold -->
<!-- SetupScreen.tsx: className="text-3xl font-bold tracking-tight"   → CHANGE TO text-xl font-semibold -->
<!-- Review.tsx:      className="text-2xl font-bold"                  → CHANGE TO text-xl font-semibold -->
<!-- HistoryView.tsx: className="text-xl font-bold"                   → CHANGE TO text-xl font-semibold (only weight changes) -->

<!-- Current outer container divs per file: -->
<!-- Home.tsx:      className="flex flex-col items-center justify-center min-h-screen bg-gray-950 text-white gap-8 p-8 text-center" -->
<!--                → ADD max-w-3xl mx-auto w-full -->
<!-- Review.tsx:    className="flex flex-col items-center min-h-screen bg-gray-950 text-white p-8 gap-6" -->
<!--                → ADD max-w-3xl mx-auto w-full -->
<!-- HistoryView.tsx: className="flex flex-col items-center min-h-screen bg-gray-950 text-white p-8 gap-6" -->
<!--                  → ADD max-w-3xl mx-auto w-full -->

<!-- SetupScreen View History button (current): -->
<!-- className="text-sm text-gray-400 underline hover:text-gray-200 transition-colors" -->
<!-- → CHANGE TO: className="text-sm text-gray-400 hover:text-white transition-colors px-4 py-2" -->
<!-- → ADD arrow: render "→ View History" instead of "View History" -->

<!-- Home footnote (current): -->
<!-- className="text-gray-600 text-xs" → CHANGE TO className="text-gray-500 text-xs" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Home.tsx and Review.tsx headings, max-width, footnote</name>
  <files>src/pages/Home.tsx, src/pages/Review.tsx</files>
  <read_first>
    - src/pages/Home.tsx (MUST read before editing)
    - src/pages/Review.tsx (MUST read before editing)
    - src/pages/Home.test.tsx (new tests from Plan 01 — these must now pass)
    - src/pages/Review.test.tsx (existing tests — verify none break)
  </read_first>
  <action>
**Home.tsx changes:**

1. h1 heading: change `className="text-4xl font-bold tracking-tight"` to `className="text-xl font-semibold tracking-tight"`

2. Outer root div: change from:
   `className="flex flex-col items-center justify-center min-h-screen bg-gray-950 text-white gap-8 p-8 text-center"`
   To:
   `className="flex flex-col items-center justify-center min-h-screen bg-gray-950 text-white gap-8 p-8 text-center max-w-3xl mx-auto w-full"`

3. Footnote paragraph: change `className="text-gray-600 text-xs"` to `className="text-gray-500 text-xs"`

**Review.tsx changes:**

1. h1 heading: change `className="text-2xl font-bold"` to `className="text-xl font-semibold"`

2. Outer root div: change from:
   `className="flex flex-col items-center min-h-screen bg-gray-950 text-white p-8 gap-6"`
   To:
   `className="flex flex-col items-center min-h-screen bg-gray-950 text-white p-8 gap-6 max-w-3xl mx-auto w-full"`

No other changes to these files in this plan. Copy changes (error message) happen in Plan 05.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run "Home|Review" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "text-4xl" src/pages/Home.tsx returns NO match
    - grep -n "font-bold" src/pages/Home.tsx returns NO match (on the h1 — other elements may keep font-bold)
    - grep -n "text-xl font-semibold" src/pages/Home.tsx returns a match
    - grep -n "max-w-3xl" src/pages/Home.tsx returns a match
    - grep -n "text-gray-500" src/pages/Home.tsx returns a match (footnote)
    - grep -n "text-gray-600" src/pages/Home.tsx returns NO match (footnote replaced)
    - grep -n "text-2xl" src/pages/Review.tsx returns NO match (on the h1)
    - grep -n "text-xl font-semibold" src/pages/Review.tsx returns a match
    - grep -n "max-w-3xl" src/pages/Review.tsx returns a match
    - npm test -- --run Home exits 0 (all 7 Home tests now pass — heading class, footnote class, max-width all green)
    - npm test -- --run Review exits 0 (existing Review tests still pass)
  </acceptance_criteria>
  <done>Home.tsx: h1 is text-xl font-semibold, footnote is text-gray-500, outer div has max-w-3xl. Review.tsx: h1 is text-xl font-semibold, outer div has max-w-3xl.</done>
</task>

<task type="auto">
  <name>Task 2: Fix SetupScreen.tsx and HistoryView.tsx headings, max-width, View History affordance</name>
  <files>src/components/SetupScreen/SetupScreen.tsx, src/pages/HistoryView.tsx</files>
  <read_first>
    - src/components/SetupScreen/SetupScreen.tsx (MUST read before editing)
    - src/pages/HistoryView.tsx (MUST read before editing)
    - src/pages/HistoryView.test.tsx (existing tests — verify none break)
  </read_first>
  <action>
**SetupScreen.tsx changes:**

1. h1 heading: change `className="text-3xl font-bold tracking-tight"` to `className="text-xl font-semibold tracking-tight"`

2. View History button: change from:
   ```tsx
   <button
     onClick={onViewHistory}
     className="text-sm text-gray-400 underline hover:text-gray-200 transition-colors"
   >
     View History
   </button>
   ```
   To:
   ```tsx
   <button
     onClick={onViewHistory}
     className="text-sm text-gray-400 hover:text-white transition-colors px-4 py-2"
   >
     → View History
   </button>
   ```

   Changes made: remove `underline`, change `hover:text-gray-200` to `hover:text-white`, add `px-4 py-2` for 44px tap target, prepend `→ ` arrow character to button text.

Note: SetupScreen's outer container div (`className="flex flex-col items-center justify-center min-h-screen bg-gray-950 text-white gap-6 p-8"`) does NOT need max-w-3xl per RESEARCH.md — the inner content (max-w-lg camera preview) already constrains it. Add max-w-3xl anyway per UI-SPEC A-11 consistency rule: "Home and SetupScreen already functionally limited by inner content but should receive the same constraint for consistency." So add `max-w-3xl mx-auto w-full` to the outer container.

**HistoryView.tsx changes:**

1. Main content h1 (line with `"Session History"`): change `className="text-xl font-bold"` to `className="text-xl font-semibold"`

2. Empty state h1 (line with `"No sessions yet"`): change `className="text-xl font-bold text-white"` to `className="text-xl font-semibold text-white"`

3. Main outer container div: add `max-w-3xl mx-auto w-full` to:
   `className="flex flex-col items-center min-h-screen bg-gray-950 text-white p-8 gap-6"`
   → `className="flex flex-col items-center min-h-screen bg-gray-950 text-white p-8 gap-6 max-w-3xl mx-auto w-full"`

   Note: The inner `max-w-2xl` on the session list div (`<div className="flex flex-col gap-2 w-full max-w-2xl">`) is correct and should REMAIN unchanged — this is the intended inner constraint per RESEARCH.md.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run HistoryView 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "text-3xl" src/components/SetupScreen/SetupScreen.tsx returns NO match (on h1)
    - grep -n "text-xl font-semibold" src/components/SetupScreen/SetupScreen.tsx returns a match
    - grep -n "max-w-3xl" src/components/SetupScreen/SetupScreen.tsx returns a match
    - grep -n "underline" src/components/SetupScreen/SetupScreen.tsx returns NO match (on the View History button)
    - grep -n "→ View History" src/components/SetupScreen/SetupScreen.tsx returns a match
    - grep -n "px-4 py-2" src/components/SetupScreen/SetupScreen.tsx returns a match (on the View History button)
    - grep -n "hover:text-white" src/components/SetupScreen/SetupScreen.tsx returns a match
    - grep -n "font-bold" src/pages/HistoryView.tsx returns NO match (on h1 elements — font-bold on other elements is OK)
    - grep -n "text-xl font-semibold" src/pages/HistoryView.tsx returns at least 2 matches (both h1 instances)
    - grep -n "max-w-3xl" src/pages/HistoryView.tsx returns a match
    - grep -n "max-w-2xl" src/pages/HistoryView.tsx still returns matches (inner constraints preserved)
    - npm test -- --run HistoryView exits 0
  </acceptance_criteria>
  <done>SetupScreen h1 is text-xl font-semibold. View History button has arrow, no underline, hover:text-white, px-4 py-2, max-w-3xl. HistoryView h1s are text-xl font-semibold, outer container has max-w-3xl.</done>
</task>

</tasks>

<verification>
Run full suite after both tasks:

```bash
cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -30
```

Expected: Home tests now all green (heading, footnote, max-width assertions). HistoryView and Review tests still pass.
</verification>

<success_criteria>
- A-04: All four pages (Home, SetupScreen, Review, HistoryView) use text-xl font-semibold on h1
- A-06: View History button in SetupScreen shows "→ View History", no underline, hover:text-white, px-4 py-2
- A-11: Home, Review, HistoryView, SetupScreen outer containers all have max-w-3xl mx-auto w-full
- A-12: Home footnote uses text-gray-500 (not text-gray-600)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-04-SUMMARY.md`
</output>
