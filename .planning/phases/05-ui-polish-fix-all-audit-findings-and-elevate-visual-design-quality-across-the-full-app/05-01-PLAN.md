---
phase: 05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/SessionListItem/SessionListItem.test.tsx
  - src/components/StorageQuotaBar/StorageQuotaBar.test.tsx
  - src/pages/Home.test.tsx
autonomous: true
requirements:
  - A-03
  - A-07
  - A-01
  - A-04
  - A-11
  - A-12

must_haves:
  truths:
    - "npm test -- --run exits 0 after this plan with all new test files passing"
    - "SessionListItem.test.tsx covers: delete button always visible (no opacity-0), delete button aria-label, score badge color classes for null/low/mid/high scores"
    - "StorageQuotaBar.test.tsx covers: critical copy string, warning copy string, critical fill color class"
    - "Home.test.tsx covers: h1 heading class, footnote text-gray-500 class, max-width container class"
  artifacts:
    - path: "src/components/SessionListItem/SessionListItem.test.tsx"
      provides: "Test coverage for A-03 and A-07 fixes"
    - path: "src/components/StorageQuotaBar/StorageQuotaBar.test.tsx"
      provides: "Test coverage for A-01 (StorageQuotaBar) and copy changes"
    - path: "src/pages/Home.test.tsx"
      provides: "Test coverage for A-04, A-11, A-12 fixes"
  key_links:
    - from: "SessionListItem.test.tsx"
      to: "SessionListItem.tsx"
      via: "render + getByRole('button', { name: 'Delete session' })"
      pattern: "aria-label.*Delete session"
    - from: "StorageQuotaBar.test.tsx"
      to: "StorageQuotaBar.tsx"
      via: "render + text queries for copy strings"
      pattern: "Storage almost full"
---

<objective>
Create the three missing test files (Wave 0 gaps) so that Plans 02–05 have automated verification for every change they make.

Purpose: Nyquist compliance — every fix must have an automated test. Three test files are missing entirely (identified in RESEARCH.md and VALIDATION.md Wave 0 gaps).
Output: Three new test files with passing assertions against current code, plus stubs for assertions that will only pass after later plans execute.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-UI-SPEC.md
@.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionListItem.test.tsx</name>
  <files>src/components/SessionListItem/SessionListItem.test.tsx</files>
  <read_first>
    - src/components/SessionListItem/SessionListItem.tsx (current component — understand current structure before writing tests)
    - src/components/ScorecardView/ScorecardView.test.tsx (test style reference — use same import/describe/it pattern)
    - src/db/db.ts (Session type — understand scorecard field shape)
  </read_first>
  <action>
Create `src/components/SessionListItem/SessionListItem.test.tsx` with the following tests:

```tsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect, vi } from 'vitest';
import { SessionListItem } from './SessionListItem';
import type { Session } from '../../db/db';

function makeSession(overrides: Partial<Session> = {}): Session {
  return {
    id: 1,
    title: 'Test Session',
    createdAt: new Date('2026-01-01'),
    durationMs: 90000,
    videoBlob: new Blob(),
    eventLog: [],
    scorecard: null,
    ...overrides,
  };
}

describe('SessionListItem', () => {
  it('renders the session title', () => {
    render(<SessionListItem session={makeSession()} onOpen={vi.fn()} onDelete={vi.fn()} />);
    expect(screen.getByText('Test Session')).toBeInTheDocument();
  });

  it('delete button is always visible (no opacity-0 class)', () => {
    render(<SessionListItem session={makeSession()} onOpen={vi.fn()} onDelete={vi.fn()} />);
    const deleteBtn = screen.getByRole('button', { name: 'Delete session' });
    expect(deleteBtn.className).not.toContain('opacity-0');
  });

  it('delete button has aria-label="Delete session"', () => {
    render(<SessionListItem session={makeSession()} onOpen={vi.fn()} onDelete={vi.fn()} />);
    expect(screen.getByRole('button', { name: 'Delete session' })).toBeInTheDocument();
  });

  it('score badge shows — and bg-gray-700 text-gray-400 when scorecard is null', () => {
    render(<SessionListItem session={makeSession({ scorecard: null })} onOpen={vi.fn()} onDelete={vi.fn()} />);
    const badge = screen.getByText('—');
    expect(badge.className).toContain('bg-gray-700');
    expect(badge.className).toContain('text-gray-400');
  });

  it('score badge shows bg-emerald-900 text-emerald-300 when overall score >= 70', () => {
    const session = makeSession({ scorecard: { overall: 75, dimensions: {} } });
    render(<SessionListItem session={session} onOpen={vi.fn()} onDelete={vi.fn()} />);
    const badge = screen.getByText('75');
    expect(badge.className).toContain('bg-emerald-900');
    expect(badge.className).toContain('text-emerald-300');
  });

  it('score badge shows bg-amber-900 text-amber-300 when overall score is 40–69', () => {
    const session = makeSession({ scorecard: { overall: 55, dimensions: {} } });
    render(<SessionListItem session={session} onOpen={vi.fn()} onDelete={vi.fn()} />);
    const badge = screen.getByText('55');
    expect(badge.className).toContain('bg-amber-900');
    expect(badge.className).toContain('text-amber-300');
  });

  it('score badge shows bg-red-900 text-red-300 when overall score < 40', () => {
    const session = makeSession({ scorecard: { overall: 25, dimensions: {} } });
    render(<SessionListItem session={session} onOpen={vi.fn()} onDelete={vi.fn()} />);
    const badge = screen.getByText('25');
    expect(badge.className).toContain('bg-red-900');
    expect(badge.className).toContain('text-red-300');
  });

  it('calls onDelete when delete button is clicked', () => {
    const onDelete = vi.fn();
    render(<SessionListItem session={makeSession()} onOpen={vi.fn()} onDelete={onDelete} />);
    const deleteBtn = screen.getByRole('button', { name: 'Delete session' });
    deleteBtn.click();
    expect(onDelete).toHaveBeenCalledTimes(1);
  });
});
```

Note: The `Session` type's `scorecard` field is `{ overall: number; dimensions: Record<string, number> } | null`. Use `dimensions: {}` for simplicity in test fixtures.

These tests will FAIL on the current component (which lacks aria-label, uses opacity-0, uses flat bg-gray-700 for all scores). They become green after Plan 02 executes the A-03 and A-07 fixes.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run SessionListItem 2>&1 | tail -20</automated>
  </verify>
  <done>SessionListItem.test.tsx exists with 8 tests. Some tests fail against current code (expected — wave 0 stubs for future fixes) and some pass. File is syntactically valid (TypeScript compiles).</done>
</task>

<task type="auto">
  <name>Task 2: Create StorageQuotaBar.test.tsx</name>
  <files>src/components/StorageQuotaBar/StorageQuotaBar.test.tsx</files>
  <read_first>
    - src/components/StorageQuotaBar/StorageQuotaBar.tsx (current component — understand how navigator.storage is used, how copy strings appear)
  </read_first>
  <action>
Create `src/components/StorageQuotaBar/StorageQuotaBar.test.tsx`. The component calls `navigator.storage.estimate()` in a useEffect, so tests must mock that API.

```tsx
import { render, screen, act } from '@testing-library/react';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { StorageQuotaBar } from './StorageQuotaBar';

function mockStorageEstimate(used: number, quota: number) {
  Object.defineProperty(navigator, 'storage', {
    value: {
      estimate: vi.fn().mockResolvedValue({ usage: used, quota }),
    },
    configurable: true,
    writable: true,
  });
}

describe('StorageQuotaBar', () => {
  it('renders null when navigator.storage is unavailable', () => {
    Object.defineProperty(navigator, 'storage', {
      value: undefined,
      configurable: true,
      writable: true,
    });
    const { container } = render(<StorageQuotaBar />);
    expect(container.firstChild).toBeNull();
  });

  it('renders the storage bar when estimate is available', async () => {
    mockStorageEstimate(500 * 1024 * 1024, 1000 * 1024 * 1024);
    await act(async () => {
      render(<StorageQuotaBar />);
    });
    expect(screen.getByText(/Storage used:/)).toBeInTheDocument();
  });

  it('shows critical copy "Storage almost full. Delete older sessions to keep recording." when pct > 95', async () => {
    mockStorageEstimate(960 * 1024 * 1024, 1000 * 1024 * 1024);
    await act(async () => {
      render(<StorageQuotaBar />);
    });
    expect(screen.getByText('Storage almost full. Delete older sessions to keep recording.')).toBeInTheDocument();
  });

  it('shows warning copy "Storage getting full. Consider deleting older sessions." when pct 80–95', async () => {
    mockStorageEstimate(850 * 1024 * 1024, 1000 * 1024 * 1024);
    await act(async () => {
      render(<StorageQuotaBar />);
    });
    expect(screen.getByText('Storage getting full. Consider deleting older sessions.')).toBeInTheDocument();
  });

  it('critical fill bar uses bg-red-500 (not bg-red-600) when pct > 95', async () => {
    mockStorageEstimate(960 * 1024 * 1024, 1000 * 1024 * 1024);
    await act(async () => {
      render(<StorageQuotaBar />);
    });
    // Find the fill div (has h-1 class and a width style)
    const fillDivs = document.querySelectorAll('.h-1.rounded-full.transition-all');
    const fillBar = Array.from(fillDivs).find(el => el.classList.contains('bg-red-500') || el.classList.contains('bg-red-600'));
    expect(fillBar?.className).toContain('bg-red-500');
    expect(fillBar?.className).not.toContain('bg-red-600');
  });
});
```

These tests will FAIL on the current component (copy strings differ, fill uses bg-red-6 currently). They become green after Plan 02 and Plan 05 apply fixes.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run StorageQuotaBar 2>&1 | tail -20</automated>
  </verify>
  <done>StorageQuotaBar.test.tsx exists with 5 tests. File is syntactically valid. Tests that check copy strings and fill color fail against current code (expected).</done>
</task>

<task type="auto">
  <name>Task 3: Create Home.test.tsx</name>
  <files>src/pages/Home.test.tsx</files>
  <read_first>
    - src/pages/Home.tsx (current component — understand structure, props interface)
    - src/pages/Review.test.tsx (test style reference for page-level tests)
  </read_first>
  <action>
Create `src/pages/Home.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect, vi } from 'vitest';
import Home from './Home';

describe('Home', () => {
  it('renders null when hasExistingSessions is true', () => {
    const { container } = render(<Home hasExistingSessions={true} onStart={vi.fn()} />);
    expect(container.firstChild).toBeNull();
  });

  it('renders the h1 heading when hasExistingSessions is false', () => {
    render(<Home hasExistingSessions={false} onStart={vi.fn()} />);
    expect(screen.getByRole('heading', { level: 1 })).toBeInTheDocument();
  });

  it('h1 uses text-xl and font-semibold (not text-4xl or font-bold)', () => {
    render(<Home hasExistingSessions={false} onStart={vi.fn()} />);
    const h1 = screen.getByRole('heading', { level: 1 });
    expect(h1.className).toContain('text-xl');
    expect(h1.className).toContain('font-semibold');
    expect(h1.className).not.toContain('text-4xl');
    expect(h1.className).not.toContain('font-bold');
  });

  it('footnote uses text-gray-500 (not text-gray-600)', () => {
    render(<Home hasExistingSessions={false} onStart={vi.fn()} />);
    const footnote = screen.getByText('Runs entirely in your browser. Nothing is uploaded.');
    expect(footnote.className).toContain('text-gray-500');
    expect(footnote.className).not.toContain('text-gray-600');
  });

  it('outer container has max-w-3xl class', () => {
    render(<Home hasExistingSessions={false} onStart={vi.fn()} />);
    // The outermost div is the page container
    const container = screen.getByRole('heading', { level: 1 }).closest('div[class*="max-w-3xl"]');
    expect(container).not.toBeNull();
  });

  it('renders "Start Recording" button', () => {
    render(<Home hasExistingSessions={false} onStart={vi.fn()} />);
    expect(screen.getByRole('button', { name: 'Start Recording' })).toBeInTheDocument();
  });

  it('calls onStart when button is clicked', () => {
    const onStart = vi.fn();
    render(<Home hasExistingSessions={false} onStart={onStart} />);
    screen.getByRole('button', { name: 'Start Recording' }).click();
    expect(onStart).toHaveBeenCalledTimes(1);
  });
});
```

These tests will FAIL on current code (h1 is text-4xl font-bold, footnote is text-gray-600, no max-w-3xl). They become green after Plan 04 applies fixes.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run Home 2>&1 | tail -20</automated>
  </verify>
  <done>Home.test.tsx exists with 7 tests. File is syntactically valid. Tests checking heading class, footnote class, and max-width fail against current code (expected). "renders null" and "Start Recording" tests pass immediately.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no existing tests were broken:

```bash
cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run 2>&1 | tail -30
```

Expected: existing tests still pass, new tests run (some fail against current code — expected for wave 0 stubs).
</verification>

<success_criteria>
- 3 new test files exist at correct paths
- All 3 files are syntactically valid TypeScript (no compile errors)
- Tests that check CURRENT behavior pass (null render, button click handlers, session title render)
- Tests that check FUTURE behavior (after Plans 02-04) are present and fail — proving they actually test the right thing
- No previously-passing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-01-SUMMARY.md`
</output>
