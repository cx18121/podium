---
phase: 05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app
plan: 03
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - src/components/AnnotatedPlayer/Timeline.tsx
  - src/components/AnnotatedPlayer/Timeline.test.tsx
autonomous: true
requirements:
  - A-02

must_haves:
  truths:
    - "Timeline container height is h-11 (44px) not h-8 (32px)"
    - "Timeline marker dots are w-4 h-4 (16px) not w-3 h-3 (12px)"
    - "Timeline marker left offset is calc(X% - 8px) not calc(X% - 6px)"
    - "Timeline marker buttons have minimum 44x44px tap area (padding expansion)"
    - "All Timeline tests pass including the updated style.left assertion"
  artifacts:
    - path: "src/components/AnnotatedPlayer/Timeline.tsx"
      provides: "WCAG-compliant touch targets on timeline markers (A-02)"
      contains: "h-11"
    - path: "src/components/AnnotatedPlayer/Timeline.test.tsx"
      provides: "Updated style.left test matching new 8px offset"
      contains: "8px"
  key_links:
    - from: "Timeline.tsx marker button"
      to: "44px tap area"
      via: "padding expansion -mx-[14px] -my-[14px] px-[14px] py-[14px]"
      pattern: "\\-mx-\\[14px\\]"
---

<objective>
Fix A-02: Timeline marker touch targets are 12x12px — below WCAG 24px minimum. Raise container to 44px, dots to 16px visual size, and expand tap area to 44x44px via padding expansion. Update the existing Timeline test that checks the style.left offset (hardcoded to 6px, must become 8px).

Purpose: Make timeline markers tappable on touch devices and accessible to keyboard users with impaired motor control. This is a HIGH severity finding.
Output: Timeline.tsx updated with larger touch targets. Timeline.test.tsx updated with corrected style.left assertion.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-UI-SPEC.md
@.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-RESEARCH.md

<interfaces>
<!-- From src/components/AnnotatedPlayer/Timeline.tsx current state: -->
<!-- Container: className="relative w-full h-8 bg-gray-800 rounded-full cursor-pointer select-none" -->
<!-- Marker button className (current): -->
<!--   "absolute top-1/2 -translate-y-1/2 w-3 h-3 rounded-full bg-amber-400", -->
<!--   "z-10 hover:scale-150 transition-transform focus:outline-none focus:ring-2 focus:ring-amber-300", -->
<!--   nearest === event ? "ring-2 ring-amber-200 scale-125" : "" -->
<!-- Marker style: { left: `calc(${leftPct}% - 6px)` } -->

<!-- PITFALL: Timeline.test.tsx line 41-43 asserts: -->
<!--   expect(buttons[0].style.left).toBe('calc(10% - 6px)'); -->
<!--   expect(buttons[1].style.left).toBe('calc(30% - 6px)'); -->
<!--   expect(buttons[2].style.left).toBe('calc(50% - 6px)'); -->
<!-- These MUST be updated to 8px when dot size changes to 16px (half of 16 = 8). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Timeline touch targets and fix the style.left test assertion</name>
  <files>src/components/AnnotatedPlayer/Timeline.tsx, src/components/AnnotatedPlayer/Timeline.test.tsx</files>
  <read_first>
    - src/components/AnnotatedPlayer/Timeline.tsx (MUST read before editing — get exact current className strings and style expression)
    - src/components/AnnotatedPlayer/Timeline.test.tsx (MUST read before editing — find the exact lines with 'calc(10% - 6px)' assertions that need updating)
  </read_first>
  <action>
**Timeline.tsx changes:**

1. Container div: change `h-8` to `h-11`:
   - From: `className="relative w-full h-8 bg-gray-800 rounded-full cursor-pointer select-none"`
   - To: `className="relative w-full h-11 bg-gray-800 rounded-full cursor-pointer select-none"`

2. Marker button className array: change `w-3 h-3` to `w-4 h-4` and add padding expansion classes for 44px tap area:
   - From:
     ```tsx
     className={[
       "absolute top-1/2 -translate-y-1/2 w-3 h-3 rounded-full bg-amber-400",
       "z-10 hover:scale-150 transition-transform focus:outline-none focus:ring-2 focus:ring-amber-300",
       nearest === event ? "ring-2 ring-amber-200 scale-125" : "",
     ].join(" ")}
     ```
   - To:
     ```tsx
     className={[
       "absolute top-1/2 -translate-y-1/2 w-4 h-4 rounded-full bg-amber-400",
       "-mx-[14px] -my-[14px] px-[14px] py-[14px]",
       "z-10 hover:scale-150 transition-transform focus:outline-none focus:ring-2 focus:ring-amber-300",
       nearest === event ? "ring-2 ring-amber-200 scale-125" : "",
     ].join(" ")}
     ```

   Explanation: dot is 16px (w-4 h-4). Padding of 14px on each side brings click area to 16 + 14 + 14 = 44px per axis. The negative margin (-mx-[14px] -my-[14px]) offsets the padding so the visual dot position does not shift.

3. Marker style.left offset: change `- 6px` to `- 8px` (half of new 16px dot size):
   - From: `style={{ left: \`calc(\${leftPct}% - 6px)\` }}`
   - To: `style={{ left: \`calc(\${leftPct}% - 8px)\` }}`

**CRITICAL: Tailwind v4 arbitrary negative margin warning:**
If `-mx-[14px]` does not apply in the browser (check DevTools), use the alternative padding-only approach instead:
- Remove the `-mx-[14px] -my-[14px]` classes
- Add `p-[14px]` to the button
- Add `overflow-visible` to the container div
Both approaches achieve 44px tap area. The primary approach uses negative margin; use padding fallback if the primary does not render.

**Timeline.test.tsx changes:**

Find the test `"each button's style.left equals the correct calc value"` and update the three assertions from `6px` to `8px`:
- From: `expect(buttons[0].style.left).toBe('calc(10% - 6px)');`
- To: `expect(buttons[0].style.left).toBe('calc(10% - 8px)');`

- From: `expect(buttons[1].style.left).toBe('calc(30% - 6px)');`
- To: `expect(buttons[1].style.left).toBe('calc(30% - 8px)');`

- From: `expect(buttons[2].style.left).toBe('calc(50% - 6px)');`
- To: `expect(buttons[2].style.left).toBe('calc(50% - 8px)');`

No other changes to Timeline.test.tsx.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run Timeline 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "h-11" src/components/AnnotatedPlayer/Timeline.tsx returns a match
    - grep -n "h-8" src/components/AnnotatedPlayer/Timeline.tsx returns NO match (on the container div)
    - grep -n "w-4 h-4" src/components/AnnotatedPlayer/Timeline.tsx returns a match
    - grep -n "w-3 h-3" src/components/AnnotatedPlayer/Timeline.tsx returns NO match
    - grep -n "8px" src/components/AnnotatedPlayer/Timeline.tsx returns a match (in the style.left calc expression)
    - grep -n "6px" src/components/AnnotatedPlayer/Timeline.tsx returns NO match (old offset removed)
    - grep -n "\-mx-\[14px\]" src/components/AnnotatedPlayer/Timeline.tsx returns a match (OR grep -n "p-\[14px\]" if fallback used)
    - grep -n "8px" src/components/AnnotatedPlayer/Timeline.test.tsx returns at least 3 matches (updated assertions)
    - grep -n "calc(10% - 6px)" src/components/AnnotatedPlayer/Timeline.test.tsx returns NO match
    - npm test -- --run Timeline exits 0 (all 7 Timeline tests pass)
  </acceptance_criteria>
  <done>Timeline container is 44px tall. Marker dots are 16px visual with 44x44px tap area. style.left uses 8px offset. All Timeline tests pass.</done>
</task>

</tasks>

<verification>
Run full AnnotatedPlayer test suite to confirm both Timeline and AnnotatedPlayer tests pass:

```bash
cd /mnt/c/Users/charl/School/cs_misc/pitch-practice && npm test -- --run AnnotatedPlayer 2>&1 | tail -20
```

Expected: all tests pass, including AnnotatedPlayer.test.tsx which uses the Timeline indirectly.
</verification>

<success_criteria>
- A-02: Timeline container is h-11 (44px)
- A-02: Marker dots are w-4 h-4 (16px visual)
- A-02: Each marker button has at least 44x44px tap area
- A-02: style.left offset updated from 6px to 8px
- Timeline.test.tsx style.left assertions updated to 8px
- All Timeline tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish-fix-all-audit-findings-and-elevate-visual-design-quality-across-the-full-app/05-03-SUMMARY.md`
</output>
